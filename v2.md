# Ironet V2 重新实现 Checklist

> 目标：不保留 V1 兼容层，以 QUIC 为唯一传输基础，重做数据面工作单元、Bulk 调度、FEC、子网转发和自动调优；保留直连、DERP、覆盖中转、NAT、Presence、路径迁移等既有能力，并加入被动可见的 SNI/直播 QUIC 流量轮廓。

## 0. 已确定的顶层决策

- [x] V2 是绿地协议，不与 V1 节点、V1 wire、V1 session ticket 互通。
- [x] 保留 QUIC，不实现自研 UDP、拥塞控制、pacing、重传或密钥交换。
- [x] 同一 QUIC 会话同时使用可靠 Stream 和不可靠 DATAGRAM。
- [x] 控制面、Presence、路由、Repair、Delivery、OAM 使用 QUIC Stream。
- [x] IP 数据、PacketTrain Cell 和 FEC parity 使用 QUIC DATAGRAM。
- [x] 不实现 ECH。
- [x] 不依赖 WebPKI 域名证书，不校验 SNI 与证书 SAN 的匹配关系。
- [x] 继续用 iroh EndpointId/raw public key 验证真实 peer 身份。
- [x] 对外可见 SNI 使用 `COVER_SNI`，对外可见 ALPN 使用 `h3`。
- [x] `network_id`、Data/Probe 类型和 V2 能力移入加密后的首个控制流。
- [x] SNI/ALPN 方案以被动观察和普通 DPI 可见面为目标；本版不实现完整 HTTP/3 应用层 façade。
- [x] Bulk 批次可以很大，但不可抢占发送 quantum 必须保持很小。
- [x] 所有可自动调优的参数由运行时闭环控制，不要求用户逐项配置。
- [x] 每个性能阶段都必须用 p2/p6 实机 iperf3 和双端 profile/火焰图验收。

### 当前实现进度

- [x] 已建立独立 `src/protocol/v2`；正式 `ironetd` supervisor 已切换为 V2-only runtime，不再启动旧 runtime。
- [x] 已实现 `SessionHelloV2`、严格二进制 Cell/Record codec、连续 PacketTrain builder 和乱序 reassembler。
- [x] `44 × 1500B` PacketTrain 在 `1382B` datagram 上压缩到不超过 51 个 Cell，测试数据利用率超过 95%。
- [x] 已 vendoring `iroh 1.0.3` 精确源码并完成 visible SNI/EndpointId 验证解耦与四维 ticket partition。
- [x] 已实现 TLS-exporter 绑定的双向 `SessionHelloV2`、network membership proof、ready transcript 和 limits 协商。
- [x] 已实现单写者 V2 TX/RX driver：真实 QUIC DATAGRAM PacketTrain、可靠控制流、有界 quantum 和多 Train 重组表。
- [x] 已实现 V2 流量分类迟滞、EDF Latency、Bulk DRR、Bulk reserve/elastic 回收和防饥饿调度。
- [x] 已实现 Cell Stripe Reed-Solomon FEC、可靠控制流 Repair codec/cache 和 loss/RTT/rate/CPU 驱动的稳定自动调优器。
- [x] 当前项目 lib 测试 `241 passed / 4 ignored / 0 failed`，其余 targets `14/1 passed`；vendored `noq-proto-v2` 为 `400 passed` 加 `3` 个 doc tests，vendored noq `36 passed / 3 ignored / 0 failed`，vendored iroh `112 passed / 1 ignored / 0 failed`，包含 endpoint 级 `test_0rtt_disabled` 与 direct+relay 同一逻辑 connection 路径共存；Clippy `--all-targets -D warnings`、fmt、V2-only gate 与 `git diff --check` 通过。
- [x] V2 运行时已完成 `h3` QUIC 端口/TUN、真实原始 GSO、自动 tune、FEC/Repair 控制流和有界队列；正式服务入口只有 `ironetd`，参数化 profile harness 已移到不安装的 `examples/ironet_v2_lab.rs`。
- [x] `ironetd` generation load 现先把产品配置严格翻译成唯一 `V2RuntimeConfig`，只接受单一双栈 bind、direct/DERP locator 和 exclusive private-link；reload/stop 通过外部 shutdown future 驱动 V2 清理，不存在协议 fallback。V2 runtime 同时发布带 `protocol_major=2` 的内存控制面快照；`doctor` 在 systemd 启动前执行同一 V2 翻译门禁。README/协议索引已改为 V2-only，`docs/protocol-v1.md` 与 `docker-v1` 发布门禁已删除。
- [x] V1 代码已从正式源码树物理删除：旧 runtime/wire/envelope/session/feature/transport/delivery/FEC/mesh/FlowRouter/observability 模块不再导出也不再存在；旧集成场景和旧 dataplane benchmark 已删除或改写为 V2。产品配置使用 `deny_unknown_fields` 并移除旧 QUIC/FEC/队列/path-selection/pairwise-secret 字段，`scripts/check-v2-only.sh` 对源码路径、wire marker、模块导出和示例配置执行发布门禁。
- [x] 正式产品建网/邀请/加入路径已接入 V2 mesh：邀请签发时把成员 EndpointId 固定为 accept-only adjacency，只有 locator 的一侧在确定性拨号失败后执行有界延迟 fallback；撤销邀请经 reload 从 V2 admission 移除，空成员产品 runtime 明确 fail-closed，绝不退化为 accept-first。未指定地址的单一产品 bind 会自动展开为同端口 IPv4/IPv6 socket 对，修复 IPv6 wildcard joiner 无法拨 IPv4 invite locator。`tests/docker-product/run.sh` 已用正式 CLI/`ironetd` 完成双栈 ping、V2 实时遥测非零、撤销和重启后拒绝重连；最新 endpoint/subnet startup canary 均报告 peer `protocol_major=2`、双端 `ready/routes_ready/connected=true`、Presence directory 为 2，Gateway/NAT/prefix 与 Kernel route 一致，且管理面快照前后一致。证据位于 `target/canary-{e,s}-1787082184/`，对应 `ironet=6b4cc376…`、`ironetd=a30bde09…`。
- [x] 正式 status/peers/CLI/TUI 已切换为 V2 原生实时遥测：直接发布 PacketTrain/Cell、Latency/Bulk、FEC/Repair、QUIC path、GSO/PMTU 与严格丢弃计数；V1 容量探测、fragment/shard/旧队列零值字段和未实现的 `metrics_file` 已删除，旧 JSON/配置字段通过 `deny_unknown_fields` 明确拒绝。
- [x] 本机双 namespace release 实测：IPv6 overlay ping 0% 丢包；单流 TCP overlay `2.115 Gbps`；3% underlay netem 下自动收敛到 `6+2`/`4+2` FEC。原始 JSON 位于 `target/v2-netns-20260817/`。
- [x] 已 vendoring `noq 1.1.1` 精确源码，增加 bounded-quantum DATAGRAM 批量 admission；Bulk-only 拥塞保持 4 Cell 上限，Latency backlog 时自动退回 1 Cell。
- [x] 本机双端 task-clock profile 已复验：批量版 `2.282 Gbps`，发送端 futex self hotspot 从 `5.46%` 降至 `4.04%`；接收端仍为 `8.06%`，下一阶段继续处理 RX 共享状态。原始 JSON、report 与火焰图位于 `target/v2-netns-20260817-batch4/`。
- [x] V2 TUN 已自动派生 network/EndpointId scoped 的 RFC 6598 IPv4 `/32` 与 ULA IPv6 `/128`，无需用户为 IPv4 subnet 手工指定隧道源地址。
- [x] 三 namespace 纯路由 Gateway 实测 `p2-sim → 11.6.1.48-sim` 为 `2.366 Gbps`、0% ping loss；与 endpoint 路径同量级。双端 profile、原始 JSON 位于 `target/v2-netns-20260817-subnet-fast/`。
- [x] profile 驱动将 V2 reassembly/scheduler/FEC/Repair/flow 表切换为 FxHash，并为完整 Record 增加直接 view 交付；RX SipHash `2.85%` 热点消失，`TrainReassembler::accept` 从 `3.12%` 降至 `2.86%`。
- [x] 已实现 SessionHello 后可靠控制流上的有界、带 generation 的双栈 route advertisement；远端自动增删 Kernel route，默认拒绝 `/0`，无需逐 peer 手工填写 subnet route。
- [x] advertised IPv4 subnet 默认自动启用 connmark + MASQUERADE，支持 `--no-subnet-nat` 纯路由；SIGTERM 在创建防火墙状态前注册并执行清理。
- [x] 三 namespace 自动路由 + NAT 验收达到 `2.346 Gbps`，LAN 无回程路由仍可通信；双端 profile 位于 `target/v2-netns-20260817-auto-nat-profile2/`。
- [x] 已实现不可变 `DataplaneSnapshotV2`、ArcSwap generation 发布、flow route lease、最长前缀首包查找和 underlay exclusion；每个 transit Cell 只做 FxHash route-label lookup，不扫描 Presence/前缀/策略。
- [x] Cell 固定头原保留 2 字节现定义为 overlay remaining-hop/hops-traversed；入口按每个 Record 的 IP TTL/Hop-Limit 分组，transit 原位递减，TTL 到期生成严格有界 OAM，错误入口/旧 epoch/立即反射均 fail closed。
- [x] mutable overlay hop shim 已从 Reed-Solomon symbol 中归一化排除，跨 transit 后的系统 Cell 与 parity 仍能正确恢复丢失 Cell；转发节点无需 FEC decode 或 Record reassembly。
- [x] one-peer runtime RX 已接入与未来 multi-peer 相同的 immutable label/local-delivery gate；本机双 namespace task-clock profile 为 `2.024 Gbps`，RX `dispatch_cell` inclusive `0.702%`，双端火焰图位于 `target/v2-netns-20260817-route-label-profile/`。
- [x] 负载中发现并修复 QUIC live PMTU 缩小时旧 Cell quantum 触发 session 退出：旧尺寸 DATAGRAM 现在按不可靠语义有界退休、累计计数，后续 PacketTrain 使用实时 maximum 重建，且不逐 quantum 放大日志。
- [x] 已实现 authenticated topology compiler：对 `(source, owner, prefix)` 确定性分配端到端 label，以健康 link/cost 编译最短路径、每跳 expected-ingress/next adjacency、直接 owner 优先和断链 transit fallback；跨 owner 重叠 prefix、重复 adjacency/link 均拒绝。
- [x] snapshot store 保留最多两个 immutable retired generation，仅在 current label unknown/stale 时回查，使旧 PacketTrain 自然排空；新 generation 禁止复用仍存活的 route epoch。
- [x] 已实现 owner Ed25519 签名、network membership tag、单调 sequence/TTL、双边 link 认证的 `PRV2` Presence；多 peer 通过可靠 Stream 原样 gossip，周期自动续租并编译/发布 immutable topology snapshot。
- [x] V2 lab harness 的 `--mesh-peer ENDPOINT_ID@SOCKET_ADDR` 已实现确定性单边拨号、一个逻辑 peer/QUIC adjacency、SNI/EndpointId 解耦握手、每 adjacency 单写者 TX 和真实 A→B→C overlay transit。
- [x] V2 RX 已支持最多三个并存 route epoch，按总内存预算分账 Reassembly/FEC，使新 snapshot 发布时旧 PacketTrain 可排空；Repair request/response 可沿 route label 反向/正向中继。
- [x] 三 namespace、两条独立 `h3` QUIC adjacency 的真实 transit profile 达到 `1.862 Gbps`，相对同机 one-peer `2.024 Gbps` 为 `92.0%`；固定 Cell shim 转发不解 Record/FEC。转发 cursor 批处理与 noq buffered receive-drain 将初版 `0.761 Gbps` 提升 `144.6%`，A/B/C 火焰图均 0 lost samples，位于 `target/v2-mesh-3node-20260817-profile7/`。
- [x] 已将每条 Presence link 的 authenticated DATAGRAM ceiling 编译为端到端 path minimum；运行中 PMTU 降低由可靠 OAM 沿 label 反向反馈，source 以 ArcSwap generation 冷更新 flow ceiling，transit 不退出 session。冷启动使用协商上限并由 OAM 快速收紧，避免 QUIC 初始 1162-byte floor 长期压低 Cell。三节点 profile 为 `1.873 Gbps`、one-peer 比 `92.5%`、较 profile7 `+0.6%`，A/B/C 0 lost samples，位于 `target/v2-mesh-3node-20260817-profile13-pmtu-oam/`。
- [x] LiveMedia 已支持网络级 `COVER_SNI` 池：按 network、无序 EndpointId 对和 profile generation 稳定自动选择，连接存续期间不变；日志只显示 profile 名称/代数，不输出所选域名。
- [x] V2 已把 IPv4/IPv6 direct 与 Tailscale DERP custom transport 合并进同一 `EndpointAddr` 和同一端到端 QUIC connection；支持 one-peer/mesh peer DERP key、多 region server 和独立持久 DERP identity。公网 `derp1.tailscale.com` 实测完成可见 `h3`、SNI/EndpointId 解耦、TLS-exporter `SessionHelloV2` 与 QUIC DATAGRAM 回环。
- [x] DERP/iroh relay selected path 被归类为可靠路径并自动关闭应用 FEC；direct↔DERP/IPv4↔IPv6 迁移只推进 connection-local tuner path epoch，不替换逻辑 adjacency、flow lease、delivery state 或 topology route epoch。noq 周期性 PathId/UDP source-port 轮换不再被误判为网络迁移。
- [x] 路径稳定性修复后的三节点直连 transit 两轮 profile 为 `1.795/1.789 Gbps`，A/B/C 均 0 lost samples，运行中 0 次错误 topology republish、0 个 `StaleRouteEpoch` 丢弃；原始 JSON、reports 与火焰图位于 `target/v2-mesh-3node-20260817-profile19-path-port-stable/` 和 `target/v2-mesh-3node-20260817-profile20-path-port-repeat/`。
- [x] LiveMedia 数据轮廓现由双向 QUIC 速率 EWMA 自动选择 `Idle`、`LiveBroadcast`、`InteractiveVideo` 或 `GenericH3Bulk`；严格 `PCV2` padding DATAGRAM 只使用 128B/512B/当前 PMTU 三档，绑定 session epoch，并按 Probe 最低优先级排在 Control/Latency/Bulk 之后。
- [x] Cover shaper 使用自动派生的 3% 上限 token bucket，最多积累 100 ms；无真实流量、真实 backlog、队列膨胀、CPU 饱和或丢包升高时预算归零，真实数据总是替代 padding，发送/接收字节与实际 overhead 均进入结构化状态日志。
- [x] RX 已将首个 DATAGRAM 后所有已到达包一次性无等待 drain 到协商上限 64，并合并成一次 TUN batch write；profile 证明缩小这个非阻塞 drain 只会增加 wakeup，因此取消了 mesh 与 one-peer RX 上无收益的动态 watch/锁读。
- [x] 自动调优状态每 10 个样本输出原因、epoch、样本年龄、RTT/loss/双向速率/PPS/队列/CPU、train/quantum/FEC/Repair、RX batch 和 cover 预算/实际开销；拥塞证据现在无条件压制 FEC/Repair，避免把 QUIC 拥塞丢包误判为可用冗余修复的随机丢包。
- [x] 最新三节点 profile32 在系统后台负载显著波动时实测 `1.515 Gbps`、6228 retrans，低于同机最佳 profile24 的 `2.016 Gbps`，不作为性能提升结论；但 A/B/C 火焰图均 0 lost samples，且运行中 FEC、实际 cover、错误 migration 和 `StaleRouteEpoch` 均为 0。完整原始数据位于 `target/v2-mesh-3node-20260817-profile32-max-nonblocking-drain/`。
- [x] 移除 one-peer RX 对本地 TX tuner 的无语义 watch 后，双 namespace 4 流 profile33 达到 `2.446 Gbps`、1157 retrans，相对旧 route-label profile 的 `2.024 Gbps` 高 `20.8%`；两端火焰图均 0 lost samples。自动 cover 实际发送 303,862B，15 秒窗口开销仅约 102 ppm，拥塞后自动归零；发送端 `memmove` 13.29%/malloc 3.88%，接收端 TUN write inclusive 21.15%/`memmove` 9.46%，下一轮热点已明确。该轮观察到一次 connection-local path epoch 变化，未影响会话或吞吐，并新增前后 locator 日志供复验。原始 JSON、reports 与火焰图位于 `target/v2-one-peer-20260817-profile33-rx-no-watch/`。
- [x] profile34–46 完成热路径结构性收敛：raw virtio TUN record 以 bounded recycler 零复制转入 `Bytes`；Cell 不再为统计重复 encode；Record 完成由 O(n²) 扫描改为覆盖字节 O(1)，Cell 去重改 bitmap，过期清扫限频并删除每 Cell LRU；fragment 存储由 BTree 节点改为有序 Vec；GSO Record 直接 gather-write 到 TUN。发送端 `CellV2::encode` 从 4.72% 降至 1.70%、用户态 memmove 从 13.29% 降至 5.94%，接收端 `accept_at` 从 3.46% 降至 0.72%，GSO gather 路径用户态 memmove 从 10.89% 降至 1.67%；稳定复测 profile42 为 `2.443/2.435 Gbps`，profile46 为 `2.415/2.406 Gbps`。原始 JSON、reports 与双端火焰图位于 `target/v2-one-peer-20260817-profile34-tun-zero-copy/` 至 `target/v2-one-peer-20260817-profile46-gather-tun-repeat/`。
- [x] noq 的 live DATAGRAM ceiling 已改为 driver 持锁更新、应用无锁读取的原子快照；首包等待与 buffered burst drain 合并为单 Future/单 connection-state 临界区。profile47 达到 `2.522/2.515 Gbps`、1466 retrans，profile48 达到 `2.527/2.519 Gbps`、1910 retrans；后者接收端 `Mutex::lock_contended` 从 5.34% 降至 4.91%，发送端从 6.79% 降至 5.28%。原始数据、reports 与双端 profile 图位于 `target/v2-one-peer-20260817-profile47-lockfree-pmtu/`、`target/v2-one-peer-20260817-profile48-single-rx-lock/`。
- [x] 已用 profile49 否证把 noq 主锁机械替换为 parking_lot 的方案：吞吐降至 `2.200/2.193 Gbps`（约 `-13%`），实现与依赖均已回退；该锁的下一步必须减少跨线程 owner 争用，而非只替换 mutex 实现。否证数据保留在 `target/v2-one-peer-20260817-profile49-parking-lot/`。
- [x] V2 Bulk DATAGRAM submission 已改为 8-quantum 浅层、driver-owned mailbox：生产者用实时原子 ceiling 前置校验并在 mailbox 满时异步背压，QUIC driver 持有主状态时批量 admission，关闭时立即释放 pending/queued Bytes。逐 quantum admission 回执的 profile50 因 producer↔driver 串行调度往返仅 `2.024/2.019 Gbps`，已删除；流水版 profile51 达到 `2.651/2.645 Gbps`、2913 retrans，较 profile48 高约 `4.9%`，发送端主锁争用从 5.28% 降至 2.74%，接收端从 4.91% 降至 3.67%。双端 perf-script 0 错误，原始 JSON、reports 与火焰图位于 `target/v2-one-peer-20260817-profile50-driver-mailbox/`、`target/v2-one-peer-20260817-profile51-pipelined-mailbox/`。
- [x] Cell wire encode 已改为 scheduler-local bounded recycling pool：`CellV2::encode_into` 覆盖复用的 `BytesMut`，allocation 由 QUIC 最后一个 `Bytes` owner 释放时无阻塞回收，避免每 Cell 重新分配大块 wire buffer。profile53 达到 `2.707/2.701 Gbps`、4164 retrans，较 profile51 接收吞吐再提高约 `2.1%`；发送端 `_int_malloc` 从 1.95% 降至 1.35%、`__libc_malloc2` 从 1.42% 降至 0.93%，双端 0 lost samples、perf-script 0 错误。原始 JSON、reports 与火焰图位于 `target/v2-one-peer-20260817-profile53-cell-buffer-pool/`。
- [x] Cell pool 不再为每个空闲 adjacency 预提交 `256 × 8 KiB = 2 MiB`：只预建 ownership descriptor，首次 encode 按实际协商 Cell 尺寸惰性扩容并保留本 adjacency 的单写者局部性。进程共享 MPMC pool 的 profile54/55 仅 `2.583/2.602 Gbps`，已否证并回退；相邻局部池 profile56 基线 `2.542 Gbps`，惰性局部池 profile57 为 `2.628 Gbps`，同时消除空闲 peer 的 2 MiB wire allocation。profile54–57 均完成双端火焰图与 0 perf-script 错误，数据位于对应的 `target/v2-one-peer-20260817-profile*/` 目录。
- [x] FEC systematic/parity Cell 也已接入独立惰性 recycling pool，geometry 自动切换只重配编码器而不再丢弃已暖 wire storage；FEC 关闭后保留无活动分配的 encoder/pool，健康路径仍直接进入普通 scheduler。20 ms RTT、A→B 2%/反向 0.1% netem 的 profile58/59 均在第 2 个样本自动进入 `4+2`，随后收敛到 `6+2`，20 样本观测 loss 为 36,679/17,060 ppm；两轮双端火焰图和 perf-script 0 错误位于 `target/v2-one-peer-20260817-profile58-lossy-fec-pool/`、`target/v2-one-peer-20260817-profile59-lossy-fec-pool-repeat/`。同时补齐 cover/probe DATAGRAM 的 live PMTU race 退休语义，最低优先级 padding 不再因路径缩小终止 session。
- [x] 可重复 QUIC Initial 可见面验收 `tests/docker-v2-sni/run.sh` 已切换为正式产品 CLI 生成/密封配置并只启动正式 `ironetd`；tcpdump 抓取两个独立 ClientHello，tshark 解密 QUIC v1 Initial并断言 SNI 仅为 `cdn.live.example`、ALPN 仅为 `h3`，不出现 `.iroh.invalid`、`ironet/ip/1` 或 probe ALPN。第二次连接保持相同 SNI/地址但期待错误 EndpointId，TLS raw-public-key 校验拒绝且 SessionHello 不激活；production daemon、正常会话和负向身份三项机器门禁均通过，pcap、TSV、状态与日志位于 `target/v2-sni-capture/`。驱动同时验证 profiling 产物 ELF magic，遇到中断链接留下的稀疏文件会 clean rebuild，避免把未启动进程误报为协议失败。
- [x] Cell/PacketTrain/FEC/Repair 已接入方向正确的累计遥测与认证控制流反馈：TX 只用对端报告的 RX recovery/waste/Repair 结果调节本方向 FEC，序列单调且拒绝旧反馈。profile63 以 `fec_tx_bytes=0` 否证逐 Record 建 train 的旧 admission；改为有界 `512 KiB` burst 聚合后，profile64 实际发送 parity 并观察到 recovery/Repair。profile65 进一步否证低速时 `8+1` stripe（train 经常不足 8 Cell），最终 profile66 的 `6+1` 在保持可编码性的同时把 `6+2` parity 减半，30 个样本中 FEC wire overhead 稳定约 `1.7%–2.1%`；profile67 健康链路保持 FEC 为零、接收 `2.589 Gbps`。profile62–67 均有双端火焰图、0 lost samples 和 0 perf-script 错误，位于对应的 `target/v2-one-peer-20260817-profile*/` 目录。
- [x] FEC encoder 删除每个 systematic Cell 的独立零填充 `Vec`：直接在 recyclable wire buffer 冻结前临时补零并归一化 routing shim，让 `reed-solomon-simd` 拷入可复用工作区后恢复真实 shim/长度；同一 encoder 通过 `reset` 跨 Stripe/geometry 复用。相同 20 ms RTT、前向 2%/反向 0.1% loss 的 profile68/69 接收 `6.391/6.741 Mbps`、378/362 retrans，均持续发送 parity、完成 recovery/Repair，双端 0 lost samples、0 perf-script 错误；相对 profile66 的 `6.029 Mbps` 未见回归，但随机丢包单轮差异不作为确定吞吐增益。产物位于 `target/v2-one-peer-20260817-profile68-fec-inplace-symbol-validated/` 和 `target/v2-one-peer-20260817-profile69-fec-inplace-symbol-repeat/`。
- [x] RX 总内存目标已由 tuner 真正接入：one-peer/mesh 通过每 adjacency 原子目标在事件边界原位调整 Reassembly/FEC 预算；缩容只按最旧 generation/Stripe 淘汰超额未完成状态，不重建 receiver，也不把 TX watch/锁带回 RX 热路径。profile70–73 暴露出原 `512 KiB` 冷启动预算在高吞吐多 train 交错时会触发 fatal hard-limit；现已把独立 RX floor 提至 `8 MiB`，内存压力改为淘汰最旧未完成 train 并累计遥测，而不是终止 QUIC session。
- [x] TX 自动 buffer 已进入 noq-proto 原生 DATAGRAM accounting：运行中 setter 在 immutable `32 MiB` hard capacity 内调整 admission limit，增容发出原生 `DatagramsUnblocked`，缩容保留已接收数据；noq driver 补齐同一 poll 中 transmit 释放预算后的 mailbox readiness 复检。profile70/71 的外层模拟实现已删除，且其“丢唤醒”初判经 profile72/73 定位后修正为上述 RX fatal pressure，并非 TX 停滞。
- [x] profile74/75 在动态 TX/RX buffer、双端 perf 与 30 秒 4 流 iperf3 同时开启时分别达到 `2.276/2.299 Gbps` 接收吞吐、1629/1573 retrans；决策值与 noq-proto 实际 admission limit 一致，RX 始终 `8 MiB`、pressure eviction 为 0，双端均 0 lost samples、0 perf-script 错误。profile75 还加入低 RTT、Bulk-only、CPU 饱和时约 8 ms 的动态 producer window，避免纯 BDP 在本机高速链路坍缩到最小值；产物位于 `target/v2-one-peer-20260817-profile74-native-quic-rx-pressure-fixed/` 与 `target/v2-one-peer-20260817-profile75-native-quic-cpu-window/`。
- [x] 新增可重复的 `/home/bubu/sdwan/scripts/profile-v2-netns.sh`：自动建立非 `200::/7` 的 IPv6 direct underlay、执行 underlay/overlay iperf3、同步采集双端 perf/report/火焰图和 summary JSON，并以同一 `kill(2)` 请求同时关闭两端，避免顺序退出制造假 dataplane 错误。profile76 的完整观测面验收达到 `2.552 Gbps`；profile77 将单 Train 的 active/completed record 哈希表改为排序小向量后达到 `2.611 Gbps`，接收端 atom-core 的 `ReassemblyTableV2::accept_at` self 从 `2.51%` 降至 `0.95%`。profile78 进一步内联 Segment 虽降低 malloc，却把 memmove 放大并使吞吐降至 `2.523 Gbps`，已按 profile 结论回退。最终 profile79 保留小表和 RX 单次严格校验，达到 `2.586 Gbps`，`CellV2::decode` atom-core self 从 profile77 的 `2.08%` 降至 `0.82%`；profile76–79 双端均 0 lost samples，完整产物位于对应的 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile*/` 目录。
- [x] V2 分类器现由协议语义而非包长保护 ICMP、TCP SYN/FIN/RST 和无 payload ACK；TCP data/GSO super-packet 仅凭尺寸不会改变 class，可靠 Control/OAM/握手天然绕过 Bulk。profile81 在该语义接线后达到 `2.620 Gbps`，双端 lost samples 为 0、无协议/会话错误；完整 JSON、report 和火焰图位于 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile81-semantic-classifier/`。profile80 同代码前一阶段复测为 `2.401 Gbps` 且接收端观测 loss 约 4%，说明本机调度噪声仍需用多轮统计而非单轮峰值下结论。
- [x] completed Record ID 追踪现使用 `<=32` 的排序小向量并在大集合时自动提升为 FxHashSet，既保持常见单 GSO Record 无哈希探测，又消除大量乱序 Record ID 的 O(n²) 插入；4,096 ID 压力测试通过。profile82 为 `2.525 Gbps`，接收端 core `TrainReassembler::accept`/`accept_at` self 为 `1.00%/1.11%`，双端 0 lost samples，未出现大集合代码侵入常见热路径。
- [x] profile83 将真实 TUN/Gateway ingress rate、Record 数/字节、平均 Record 大小和 GSO ingress ratio 接入每 adjacency 状态：发送端实测 `305.1 MB/s`、平均 `59,861B`、GSO 比例 `999,189 ppm`，反向 ACK 方向为 `250 KB/s`、平均 `52B`、GSO 为 0，方向语义正确；overlay `2.430 Gbps`，双端 0 lost samples、无协议/会话错误。完整产物位于 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile83-ingress-telemetry/`。
- [x] profile84 将上述遥测接入自动 TUN admission batching：约 60 KiB、99.9% GSO 的数据方向由冷启动 64 自动收敛为 8 条（约 480 KiB），52B ACK 方向保持 64 条以继续合并唤醒；无需用户配置，单次仍无等待且受 512 KiB 字节硬上限约束。实测 overlay `2.489 Gbps`、双端 0 lost samples、无协议/会话错误，产物位于 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile84-auto-tun-batch/`。
- [x] 跨 Cell Record 的前两片 descriptor 现以内联 SmallVec 保存，常见 1,500B/两 Cell 路径不再为 offsets/fragments 各分配一次；超过两片的 GSO Record 自动 spill，保持既有 gather-write。单元测试直接断言两片完成记录未 spill；profile85 的约 60 KiB GSO 主路径为 `2.397 Gbps`、双端 0 lost samples、0 protocol errors。新增 benchmark-only offload 开关后，profile86 实际生成平均 `1,499B`、GSO ratio 0、`847 Cell/MB` 的两片流，达到 `519.8 Mbps` 且双端 0 lost samples；产物位于 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile85-two-fragment-inline/` 与 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile86-two-fragment-no-gso/`。
- [x] TUN ingress 的 Record/字节/GSO 五个 relaxed atomic 更新现先在 admission batch 内用局部值归并，一次 batch 每项最多写一次；mesh 按 adjacency 分桶后再归并，避免把任一节点的遥测记到错误路径。profile87 达到 `2.613 Gbps`，数据方向平均 Record `60,165B`、GSO ratio `999,260 ppm`，双端 perf lost samples 为 0、协议错误为 0；完整 JSON、report 与火焰图位于 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile87-batched-ingress-metrics/`。吞吐增量仍只作为热路径无回退证据，不用单轮本机噪声宣称收益百分比。
- [x] Cell decoder 对 continuation 的零长度 metadata 直接构造 canonical empty `Bytes`，不再用 `slice(n..n)` 让每个 GSO continuation Cell 保留/克隆整块 QUIC DATAGRAM allocation；非空 metadata 仍是零复制 slice。profile89 复验达到 `2.471 Gbps`，双端 perf lost samples 为 0、无协议错误，完整产物位于 `/home/bubu/sdwan/target/v2-one-peer-20260817-profile89-empty-metadata-no-clone-verified/`；profile88 因接收端 perf lost 68 samples 不作为验收样本。
- [x] 低损链路上 `stripe_id == 0` 的系统 Cell 现直接进入 reassembly，不再经过 FEC decoder 的整 DATAGRAM clone、单元素 output Vec allocation 与 stripe expiry；带 stripe 的 data/parity 和 Repair 仍走完整 FEC 验证。profile91 在 `499 Hz` 双端 task-clock profile 下达到 `2.589 Gbps`，双端 0 lost samples、0 protocol errors，接收端 report 中 `CellStripeDecoder::push_at/expire` 已完全退出热路径；完整产物位于 `/home/bubu/sdwan/target/v2-one-peer-20260818-profile91-unstriped-fec-bypass-verified/`。profile90 的代码相同但接收端 perf lost 271 samples，已排除；profile 脚本新增可记录的 `IRONET_V2_PROFILE_FREQUENCY`，以便在采样带宽不足时降低频率而不是接受丢样本。
- [x] 每个 route epoch 现单写复用 Cell decoder 的 `Vec<RecordSegment>` allocation，reassembly 以 drain 移走 QUIC-owned payload views 后立即把空 descriptor storage 交还 epoch；不建立跨线程 payload pool，也不增加引用计数。容量复用单测与真实 FEC PacketTrain 回归均通过。profile92 达到 `2.594 Gbps`、双端 0 lost samples/0 protocol errors；对比同为 499 Hz 的 profile91，接收端 Atom-core self 中 memmove `3.88%→2.31%`、Cell decode `2.74%→0.82%`，完整产物位于 `/home/bubu/sdwan/target/v2-one-peer-20260818-profile92-rx-segment-storage-reuse/`。
- [x] 已用当前精确静态产物 `sha256:cfe7898a9fa844caf62e6d4d5d7de3d3d14c4950d99471912758c698bc99d70d` 完成 `eql12 → wuwei-ws` 公网运营商 IPv6 预验收，underlay 明确为 `2408:820c:… → 2408:8207:…`，不经过 `200::/7`。同方向 4 流 15 秒 underlay 为 `53.002 Mbps`，4 流 30 秒 V2 overlay 为 `42.599 Mbps`，比值 `80.37%`；overlay 记录 1,624 次 TCP retrans，3 次 OAM ping 丢 1 次，说明当前差距首先由真实链路随机/burst loss 与 QUIC cwnd 收缩主导，而非 CPU 饱和。自动 FEC 在负载初段进入 `4+2`，随后按对端反馈降冗余/关闭；对端累计恢复 48 Cell、浪费 2,751 parity、请求 234 Repair，发送端实际 FEC wire overhead 峰值仅 `6.7%`。发送/接收端 CPU 分别约 `44–51%`/`18–25%`，P-core self hotspot 为 `memcpy 7.22%`，接收端 `memcpy 3.51%`，两端 perf lost samples 均为 0、protocol errors 为 0。完整 iperf JSON、自动 tune 日志、reports、双端火焰图和 `summary.json` 位于 `/home/bubu/sdwan/target/v2-wan-local-wuwei-20260818-profile96-final/`。这是一轮真实 WAN 预验收，不替代 p2 → wuwei-ws 的最终命名门槛。
- [x] 实机预验收同时识别并排除了测试宿主的透明代理干扰：本机 `sing-box` 会接管 V2 自动地址所在的 `100.64.0.0/10` TCP benchmark socket，使未打标的 iperf 控制连接不进入 `v2wan0`。profile96 仅对测试进程设置既有 bypass `SO_MARK=0x2024`，并用 `--bind-dev v2wan0` 固定出接口；Ironet 进程、QUIC 公网 IPv6 路径和产品数据面均未绕过或修改。后续实机脚本必须先检测此类 host policy，避免把代理接管误报为 overlay 故障。
- [x] V2 Repair 现具备完整、有界的请求生命周期：最多 4,096 个 pending request、2 秒 TTL、request-id wrap/collision 防护、`request_id + RepairKey` 严格双匹配、重复/过期 response 非致命忽略、错误 key fail closed；只有匹配且 route epoch 仍活跃的 Cell 才能进入 reassembly。方向反馈 wire 增加 matched request 数、matched requested Cell 数和累计 request→response latency，避免跨 1 秒反馈边界时命中率分母错位；状态同时输出平均/最大 RTT、stale response 和累计完成数。路径迁移会清空 tuner 的 Repair 置信度，至少 3 个新路径观测后才允许据此降冗余。
- [x] `scripts/profile-v2-netns.sh` 现支持可重复的 A→B/B→A 独立 netem delay/loss；历史验收使用 499 Hz，当前为保护宿主管理面默认使用 99 Hz，并在 Intel hybrid 主机自动从 `cpu_core/cycles` 生成代表实际 worker 的 P-core 主火焰图（仍保留 Atom/raw perf artifacts）。当前精确静态产物 `sha256:6729e52814eaab0b7fe4e42e0b496b9c63db272394a5da16ca25b71f24bdffb2` 在 `A→B 20 ms/2% loss、B→A 20 ms/0.1% loss` 下完成双端验证：profile99 underlay/overlay 分别 `19.122/12.425 Mbps`，两端 perf lost samples、protocol errors、stale Repair 均为 0；接收端累计完成 8 个 Repair request、命中 30/30 Cell，最大 response latency `51.092 ms`。P-core flamegraph 位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile99-bbr3-thin-fec/{a,b}.svg`。
- [x] 丢包 profile 明确证明首要瓶颈是 QUIC loss-based cwnd 而非 memcpy/加密：同一 netem 下 CUBIC profile97 overlay 仅 `4.010 Mbps`（underlay `14.529 Mbps`，比值 `27.60%`）；V2 自动采用 QUIC BBR3 后 profile98 overlay 达到 `12.876 Mbps`（underlay `16.803 Mbps`，比值 `76.63%`），绝对 overlay 提升 `3.21×`，controller pacing/send-quantum/cwnd 已进入每路径状态。随后 matched Repair 证明命中率 `100%` 且响应约 `45 ms` 时，中度丢包从 `6+1` 自动稀疏为 `8+1`；profile99 相对 profile98 的累计 FEC wire/data 从 `30.5‰` 降至 `20.4‰`（约 `-33%`），overlay 单轮 `12.425 Mbps` 与 profile98 的差异保守视为线路/控制器采样噪声，不宣称吞吐增益。完整 JSON、logs、reports 和双端火焰图位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile97-repair-autotune/`、`profile98-bbr3-repair-autotune/`、`profile99-bbr3-thin-fec/`。
- [x] BBR3 的本机 clean-path 回归已通过 profile 反证后按路径自动修正，而不是新增用户配置：无 netem 的 profile100 因 sub-ms userspace pacing 只有 `1.324 Gbps`；min-RTT `<1 ms` 时自动旁路 pacing timer 的 profile101 为 `1.731 Gbps`，再把该模式下 cwnd floor 自动提高到 `512 KiB` 的 profile102 为 `1.833 Gbps`，相对 profile100 恢复 `38.4%`。WAN 仍保留完整 BBR pacing；相同 `20 ms/2%` 前向、`20 ms/0.1%` 反向 netem 的 profile103 明确记录非零 pacing rate，overlay `14.276 Mbps`，两端 0 lost samples、0 protocol errors。该自动旁路只在 QUIC 控制器内部依据实测 min-RTT 生效，不向用户暴露新旋钮。
- [x] RX common path 现把连续到达的当前 PacketTrain 保存在单写者 hot slot：同一 train 后续 Cell 不再反复查询 completed tombstone/hash table，交错 train 才退入有界 FxHashMap；route snapshot 已解析的固定 Cell shim 同时直接交给 payload decoder，local delivery 每 DATAGRAM 只做一次固定头校验。profile104 的 Atom-core `ReassemblyTableV2::accept_reusing_at` self 从 profile102 的 `5.25%` 降至 `2.63%`，但 clean overlay `1.783 Gbps` 相对 profile102 的 `1.833 Gbps` 没有形成吞吐增益，因此这里只记录 CPU 路径收敛、不宣称带宽提升；双端 P-core 火焰图、raw perf 与 JSON 位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile104-hot-reassembly-route-parse/`。
- [x] 当前精确静态产物 `sha256:c56a907791e3c429df84c004e9e6963a61950ebc57334dcf870d2d4af1d0de71` 再次完成 `eql12 → wuwei-ws` 真实运营商 IPv6 profile105：路径为 `2408:820c:… → 2408:8207:…`、明确不经过 `200::/7`；4 流 underlay/overlay 分别 `50.886/39.627 Mbps`，比值 `77.87%`，本轮线路本身已有 941 次 underlay retrans，overlay 为 4,113 次。自动 FEC 负载初段发送约 `5.998 MB` parity 后在 Repair 证明有效时关闭；接收端 5,151 parity 仅恢复 16 Cell，随后 30 个 Repair request 命中 `69/69` Cell、最大响应 `69.577 ms`、stale response 为 0。两端 protocol errors/route drops/perf lost samples 均为 0，双端报告、火焰图和 `summary.json` 位于 `/home/bubu/sdwan/target/v2-wan-local-wuwei-20260818-profile105-bbr-hotrx/`。这仍是 eql12 预验收；p2/p6 名称在本机不可解析，已知 p2 公网 IPv6仍拒绝当前 workspace key，因此不替代 p2 命名门槛。
- [x] profile109 将仅在实测 minimum RTT `<1 ms` 时生效的 BBR3 flight floor 从 `512 KiB` 提升到一个完整的自动 send-buffer turn（`2 MiB`），避免高包率 direct path 因 ACK/runtime wakeup 抖动瞬间排空；clean overlay 达到 `1.963 Gbps`，相对同代码路径 profile104 的 `1.783 Gbps` 提升 `10.1%`、相对 profile102 的 `1.833 Gbps` 提升 `7.1%`，双端 perf lost samples 与协议错误均为 0。profile110 随后在 `A→B 20 ms/2% loss、B→A 20 ms/0.1% loss` 下确认该 floor 自动退出：minimum RTT 约 `40 ms`、BBR pacing 维持非零，overlay `15.536 Mbps`；接收端 FEC 收到 444 parity、恢复 23 Cell，Repair 命中 `39/39` Cell并完成 7 个请求，stale response 为 0。精确静态产物 `sha256:ae2e871a4af21e8a0bf6d82eaa39e5caa9749a1290a16274c244d553920a4426` 的双端 P-core 火焰图、raw perf 与 JSON 位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile109-bbr3-2m-lowrtt-cwnd/` 和 `/home/bubu/sdwan/target/v2-netns-20260818-profile110-bbr3-2m-lossy-regression/`。
- [x] QUIC socket RX 的 profile106–108 还验证并回退了“把 GRO-sized receive slot 直接转交协议层”的方案：全量 ownership transfer 因每包重新清零约 94 KiB slot 出现 `6.48% calloc`，即使改成未初始化或只转交 `>=16 KiB` GRO 聚合，clean overlay 也只有 `1.553/1.650/1.594 Gbps`，均低于保留稳定 socket slab、按实际 `meta.len` 精确复制的 profile104。最终代码不保留这条以更大 allocation/cache footprint 换一次 memcpy 的负优化；原始双端火焰图保存在对应 profile106–108 目录，防止后续重复试错。
- [x] 新增可重复的 `/home/bubu/sdwan/scripts/profile-v2-subnet-netns.sh` 固定三 namespace 回归：A 自动学习 B 发布的 `11.6.1.48/32`，B 使用多队列 TUN + GSO/GRO 纯路由，LAN host 显式经 B 回程；同一精确产物依次测 underlay、双向 endpoint overlay 和双向 subnet，并同步覆盖两端 P-core profile。首次运行发现 `--no-subnet-nat` 仍执行 iptables cleanup；现已从生命周期上修复为纯路由只开启内核 forwarding，完全不创建/清理 NAT guard。profile112 双向 ping 均 0% loss，endpoint 正/反向为 `1.885/1.841 Gbps`，subnet 正/反向为 `1.942/1.868 Gbps`（分别为 endpoint 的 `103.0%/101.5%`），两端 perf lost samples、protocol errors、route drops、ERROR/WARN 均为 0。精确静态产物 `sha256:50deeccfa7f1b3f7f1f49cc7d090a928de92b08915ed7d6dfcf8c695db5d0491` 的 JSON、logs、reports 与双端火焰图位于 `/home/bubu/sdwan/target/v2-subnet-netns-20260818-profile112-pure-route/`；本地固定回归已证明 Gateway 没有额外吞吐瓶颈，p2/11.6.1.48 三轮命名实机门槛仍独立保留。
- [x] 同一 Gateway 脚本现覆盖双栈纯路由与可选 NAT 模式：profile113 的 IPv4 subnet 正/反向为 endpoint 的 `104.9%/98.9%`，IPv6 为 `106.4%/99.9%`，四向 ping 均 0% loss；NAT 模式刻意不向 LAN host 安装任何 overlay 回程路由，profile114 仍完成 IPv4/IPv6 正向 ping/iperf，并断言 `IRONET_V2_NAT_OUT` 的 MASQUERADE/NAT66 conntrack 首包计数均非零。profile114 同时暴露 8 MiB RX 总预算被固定预分给 3 个潜在 route epoch、再平分 Reassembly/FEC 后每个活跃组件实际仅 `1.33 MiB`，在连续高速矩阵中两端累计 7/6 次 pressure eviction。现改为按实际活跃 epoch 数动态重分账：单 epoch 各获 4 MiB，第二/第三 epoch 激活时才有界收缩；真实 pressure delta 还会自动将总预算快速升档并保持 10 个样本后缓慢回收，不增加用户参数。精确静态产物 `sha256:992eb27cc11649b5698df503a36b7e170da2f18b0d062b31ecdfb176e252450f` 的 profile115 在相同双栈 NAT 矩阵中全程保持 8 MiB 且 pressure eviction 降为 `0/0`，IPv4/IPv6 subnet 为 endpoint 的 `102.4%/107.5%`；profile116 进一步复验双栈纯路由四向均为 endpoint 的 `97.1%–102.6%`、四向 ping 0% loss、两端全 50 个样本 pressure eviction 为 0。两轮均为 0 lost samples、0 protocol errors、0 route drops、0 ERROR/WARN；profile113–116 的 JSON、NAT 计数、reports 与双端火焰图位于对应 `/home/bubu/sdwan/target/v2-subnet-netns-20260818-profile*/` 目录。
- [x] profile117 用 Intel LBR 对此前无法展开的 `memcpy` 做了逐调用链归因：发送端 12.25% 总采样中的 5.66% 来自 QUIC packet/frame encode、2.55% 来自逐包 tracing 字段格式化、1.65% 来自 V2 Cell encode、1.14% 是 AEAD 固定尾部移动；接收端 12.78% 中 2.27% 是 socket slab 的实际长度精确复制、1.75% 是接收交接、1.57% 是 AEAD 固定尾部移动、1.48% 是 QUIC decode。由此未重复 profile106–108 已否证的大 slot ownership transfer，也未把 QUIC AEAD 所需连续 packet encode 伪装成可删除复制。真正无语义的成本是 iroh `poll_send` 在默认 `RUST_LOG=info` 下为每个 packet 创建 INFO span；降为 TRACE 后 profile119 的发送端 `new_span` 从 6.32% 降至 0、tracing formatting 从 7.40% 降至 0.36%，同为 LBR 的 clean overlay 从 profile117 的 `1.864 Gbps` 提至 `1.978 Gbps`。完整调用链、reports 与双端火焰图位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile117-lbr-memcpy-attribution/` 和 `profile119-lbr-trace-hot-span/`。
- [x] noq endpoint→connection 的 RX 事件现按同一 socket receive batch、同一 ConnectionHandle 合并为一次有界 MPSC 投递；单事件保持无额外 Vec allocation，多连接 socket 才分配 fallback 索引，不新增用户参数或等待窗口。精确静态产物 `sha256:484ed74e96995678f976b10717146918cfee19981d6673ec708663a8a35744a8` 的 profile120 在与 profile119 相同的 `499 Hz` LBR 条件下达到 `2.212 Gbps`（`+11.8%`），接收端 channel-event `memcpy` 从总采样 `1.30%` 降至 `0.94%`，总 `memcpy` 从 `11.49%` 降至 `10.24%`；独立 DWARF 重复轮 profile121 为 `2.175 Gbps`，较同调用图 profile118 的 `1.976 Gbps` 高 `10.1%`。profile122 又在 `A→B 20 ms/2% loss、B→A 20 ms/0.1% loss` 下完成 `12.526 Mbps` 丢包回归：接收端收到 4,461 parity、恢复 93 Cell，30 个 Repair request 命中 `114/114` Cell，最大响应 `44.601 ms`、stale response 为 0。profile120–122 双端均 0 perf lost samples、0 protocol errors、0 route drops、0 pressure eviction、0 ERROR/WARN；原始 JSON、logs、reports 与双端火焰图位于对应 `/home/bubu/sdwan/target/v2-netns-20260818-profile*/` 目录。
- [x] RX event batch 的第二个事件现在按当前 `RecvMeta` 中已知的 GRO stride 数一次预留、并以 160 events 为内存硬上限，不再从容量 2 几何增长。精确静态产物 `sha256:9ab16c2e31a5d29c5fc5ecae1d14d020e98f779fafc04cb7dc1257ae33cc20ac` 的 profile125 在 LBR 下将接收端 `Vec<ConnectionEvent>::grow_one/realloc` 从 profile120 的总采样 `0.350%` 降至 `0.084%`，clean overlay 为 `2.320 Gbps`（同调用图 `+4.9%`）；独立 DWARF profile126 为 `2.230 Gbps`，较 prealloc 前 profile121 高 `2.5%`。同 SHA 的 profile127 进一步完成无并发编译干扰的双栈 Gateway 纯路由矩阵：IPv4 endpoint/subnet 正向 `2.190/2.155 Gbps`（`98.4%`）、反向 `2.291/2.405 Gbps`（`105.0%`），IPv6 正向 `2.323/2.295 Gbps`（`98.8%`）、反向 `2.441/2.383 Gbps`（`97.6%`）；四向 ping 0% loss，两端 0 perf lost samples、0 protocol errors、0 route drops、0 pressure eviction。profile125–127 的 JSON、logs、reports 与双端火焰图位于对应 `/home/bubu/sdwan/target/v2-{netns,subnet-netns}-20260818-profile*/` 目录。
- [x] 身份文件的父目录生命周期已修正：原实现会在创建 identity 时无条件把既有父目录 `chmod 0700`，实机蓝绿测试把共享 `/tmp` 从 `1777` 改成了 `0700`。普通 EndpointId identity 与 DERP identity 现都用带 `0700` mode 的递归 `DirBuilder`，只约束本次新建目录，绝不修改既有共享目录；两组回归测试同时断言父目录 `0755` 保持不变且 key 仍为 `0600`。修复后的精确静态产物 `sha256:ba55991b29d1f022865a0b9b2ace5045b553078e0e4b23ee8dfd1cb8a4e4d9c4` 在 profile129 达到 `2.253 Gbps`，双端 perf lost samples、protocol errors、route drops 均为 0，测试前后本机与 wuwei-ws 的 `/tmp` 都保持 `1777`；完整本机 JSON、reports 与双端火焰图位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile129-identity-parent-fix/`。
- [x] 同一精确产物又完成 `eql12 → wuwei-ws` 真实运营商 IPv6 profile132：公网路径固定为 `2408:820c:… → 2408:8207:…`，不经过 `200::/7`，underlay/overlay 为 `51.541/33.001 Mbps`（`64.03%`）。本轮 underlay 自身已有 4,799 次 TCP retrans，overlay 为 1,370 次；接收端累计收到 1,567 parity、恢复 8 Cell，Repair 最大响应已到 `1.893 s`，表明当前差距首先是这轮强随机/burst loss 下的恢复时效与 QUIC inflight，而不是 CPU 饱和。两端 perf lost samples、protocol errors、route drops 均为 0，并保留完整 P-core/raw perf 与双端火焰图。profile130 的双端抓包还直接证明未标记的 `100.64/10` benchmark 被宿主透明策略二次路由到 `tun0`；profile131 使用既有 bypass mark/绑定 V2 TUN 后 20/20 ping 到达，profile132 因此对 iperf socket 显式使用测试专用 `SO_MARK=0x2024 + SO_BINDTODEVICE=v2w132`，没有修改 Ironet/QUIC 数据面。原始抓包与最终验收前样本位于 `/home/bubu/sdwan/target/v2-wan-local-wuwei-20260818-profile{130-rx-debug,131-marked-route-debug,132-marked-profile}/`；这仍不替代 p2 命名门槛。
- [x] V2 启动生命周期现覆盖数据面任务建立前的无限等待区：one-peer 的 accept/dial、SessionHello 以及 mesh 的全 adjacency 建连/协商都与 SIGINT/SIGTERM 竞争，信号分支主动关闭 endpoint/connection 后干净返回；被动端没有 peer 时不再等到强制 KILL。普通与 mesh 被动端进程级回归都确认 SIGTERM 被启动阶段立即观察并正常退出，同时复核 identity 父目录权限不变。
- [x] profile132 的 Repair 异常已定位到两个耦合错误，而不是把 `received=24`、`completed_requested=4759` 误判为计数损坏：可靠 Control Stream 上同一 Stripe 每 50 ms 重发请求，且 FEC 迟滞仍保留 geometry 时 tuner 可能先把 Repair cache 设为 0；FEC 关闭又会立即清空已经向接收端承诺可修复的 Cell。V2 现对每个 Stripe 只发一次可靠 Repair，请求中的空/部分 response 视为 sender cache 的最终快照；cache 降档只停止新 admission，既有 Cell 保留到原 2 秒 TTL；Repair 容量从最终 FEC geometry 重新计算，杜绝 `fec=Some/cache=0`。精确产物 `sha256:527699bc4c81c51f626438078ba8692048669963dd60752e3b468cb44f1a1fc2` 的真实运营商 IPv6 profile134 将同类计数收敛为 46 个请求、`117/117` Cell 命中、最大 `71.439 ms`，对比 profile132 的 2,111 个请求、`24/4759` 命中和 `1.893 s` 最大响应，消除了 Repair request amplification 与 cache self-eviction；双端仍为 0 perf lost samples、0 protocol errors、0 route drops。
- [x] 严重 loss 初始仍保留 `4+2` 快速保护，但当方向反馈证明 Repair 命中率 `>=80%`、响应不超过 `max(4×RTT,100 ms)`、parity 浪费 `>80%` 且 recovery `<10%` 后，自动降为 `6+1`，无需用户参数。最终精确静态产物 `sha256:122b5d07e21181ea6474dcf8f0a810b2044be16d201fddc7631d7d14e2897096` 的 clean profile135 为 `2.173 Gbps`；真实运营商 IPv6 profile136 的 underlay/overlay 为 `52.531/41.724 Mbps`（`79.43%`），Repair 完成 112 个请求并命中 `320/320` Cell、最大 `60.436 ms`。与 underlay 几乎相同的 profile134 相比，本轮 Repair 负载更重，FEC TX 仍从 `9.157 MB` 降到 `7.159 MB`（`-21.8%`），收到 parity 从 7,667 降到 5,807（`-24.3%`）；overlay 单轮从 `28.716` 升到 `41.724 Mbps` 只作为同路径观测，不把随机 WAN 样本宣称为确定代码增益。profile135/136 两端火焰图、flat/raw reports、iperf JSON 均完整，perf lost samples、protocol errors、route drops 全为 0，位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile135-repair-fec-final/` 与 `/home/bubu/sdwan/target/v2-wan-local-wuwei-20260818-profile136-repair-fec-final/`；p2/p6 命名门槛继续保留。
- [x] 自动调优决策删除了未接入数据面的 `lanes` 和 `wakeup_batch` 两个伪输出：V2 DATAGRAM 只保留一条 QUIC connection 与一个无等待 `receive_batch=64` burst drain，避免用户或状态机调节实际不生效的参数。
- [x] V2 endpoint 现从 TLS client/server 两侧彻底禁用 0-RTT：client 保留分区 ticket 做完整 resumption handshake 但不发送 early data，server 宣告 `max_early_data_size=0`；首次与恢复连接的独立 iroh 集成测试均确认 `into_0rtt` 不可用，所有数据面状态只在完整握手与 SessionHello 后建立。
- [x] 新增两个 ASan/libFuzzer target：覆盖全部 V2 wire decoder 以及有状态 CellStripe/Reassembly 序列；有效 corpus 的 wire/stateful 覆盖从空 corpus 的 203/143 提升到 821/580 edges，首轮各 1,000 次 smoke 通过。GSO 保留路径同时新增真实 `gso_input_bytes`、`gso_preserved_bytes`、`gso_fallback_splits` 状态字段与 checksum-only 负向计数测试。
- [x] 真实机前置审计已从 `wuwei-ws` 通过三大运营商 IPv6 复核：p2 direct endpoint 为 `[2409:8a00:24a0:7820:fd85:9ed0:3fde:d0ff]:4000`，10 次 overlay OAM ping 为 0 丢包、平均 `8.226 ms`；采样时旧 V1 累计 `957/94476` path loss、`2+2` FEC 收到 51,341 recovery shards 却仅恢复 199、过期 5,273 blocks、reassembly eviction 1,683，说明真实验收必须重点验证 V2 的方向反馈和低冗余收敛。原始状态保存在 `target/v2-real-host-audit-20260817/`。最新访问复核中，工作区以 `root`/`bubu` 直连 p2、p6 的运营商 IPv6 都在 SSH public-key admission 被拒绝；此前经 `wuwei-ws` 到 p2 同样被拒绝、到 p6 超时。未变更远端进程、路由或负载，审计与本次正式 V2 binary SHA 位于 `target/v2-real-access-audit-20260818-v2only/`。
- [x] V2 已建立按同一状态窗口闭合的 TX 字节账本：从 QUIC `udp_tx.bytes` 总量中分别列出原始 Record、PacketTrain metadata、Cell envelope、FEC、Repair request/response、其他可靠 Control、padding 与 QUIC packet/frame/AEAD/ACK/retransmission residual；Control/Repair 同时有方向独立的累计 RX/TX byte counter。若 QUIC 异步 serialization 跨越日志窗口，差额单列为 `interval_accounting_lag_bytes`，不伪装成协议 overhead。精确静态产物 `sha256:101ce23a7d7a288d97c20fac4f004ee057e0034386cec2978eb50578a71b1900` 的 clean profile137 在主发送方向记录 `2.866 GB` QUIC UDP payload、`2.695 GB` Record、`33.814 MB` PacketTrain metadata、`74.454 MB` Cell envelope、`62.430 MB` QUIC residual，窗口 lag 为 0，Cell/QUIC residual 分别为 `26‰/21‰`；overlay 为 `2.072 Gbps`。同 SHA 的 profile138 在 `A→B 20 ms/2% loss、B→A 20 ms/0.1% loss` 下为 `54.174/13.847 Mbps` underlay/overlay，明确观测 `561,092 B` FEC、`331 B` Repair request 和 `28,565 B` Repair response；收端完成 7 个 request、命中 `22/22` Cell、最大 `54.504 ms`，双向 counter 严格对应。两轮双端 DWARF/P-core 火焰图、raw/flat reports、iperf JSON 和带 binary SHA 的 summary 均完整，perf lost samples、protocol errors、route drops 全为 0，位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile137-wire-accounting-clean/` 与 `/home/bubu/sdwan/target/v2-netns-20260818-profile138-wire-accounting-loss/`；profile 脚本以后自动把精确 binary SHA 写入 `binary.sha256` 和 `summary.json`。
- [x] 真实 WAN 的极端丢包回归继续下钻到 QUIC BBR3，而不是增加用户参数：loss threshold 现以实测 minimum/smoothed RTT 自动区分排队丢包和无排队随机丢包，ECN/RTT inflation 始终保留标准 2% 阈值；同时修复 `process_lost_packet` 在判定前读取旧 rate sample 以及 `inflight_at_loss` 无符号下溢。精确静态产物 `sha256:43eb7f13649bf2500b4a628a13003873ffdf817b68990826f58f24ee94aba258` 的 profile140 在 `20 ms/2%` 前向 loss 下 overlay 为 `15.432 Mbps`，profile141 clean 为 `2.247 Gbps`，双端火焰图均 0 lost samples；单轮差异只作为无回归证据，不把随机线路样本宣称为确定增益。
- [x] profile142 随后暴露了更深的 QUIC idle-restart 根因：运营商 IPv6 underlay 仍有 `52.649 Mbps`，但连接先经历 15 秒 underlay 基线/Control-only 空闲后，首个 Bulk send 才进入数据面；原 `on_packet_sent` 在把 `inflight` 加到非零后才调用 BBR idle predicate，使该条件永远不成立，连接被留在 ProbeRTT，发送 cwnd 从 `4,800 B` 仅缓慢长到 `9,446 B`，40 秒 overlay 只有 `1.658 Mbps`。现按 pre-send inflight 执行 idle restart，首个数据包会先退出过期 ProbeRTT、恢复 prior cwnd 再计入 flight。最终精确静态产物 `sha256:7c2beb2e84fbd1113280c78cd5a2a32a956fe5e7ce97c12ae75b2d69b68f80f1` 的 `eql12 → wuwei-ws` profile143 固定运营商公网 IPv6 `2408:820c:… → 2408:8207:…`、明确不经过 `200::/7`，underlay/overlay 为 `52.368/40.308 Mbps`（`76.97%`）；Bulk 期间 cwnd 达 `147,047 B`，不再停在 min-pipe。相对紧邻 profile142 的 overlay 恢复 `24.31×`；两轮 underlay 吞吐接近，但 profile142/143 retrans 分别为 5,924/1,535，因此该倍数同时包含线路 loss 变化，代码因果由 idle predicate 单测和 cwnd 时间线闭合。profile143 的 40 秒 iperf 完整结束、OAM ping 0% loss、两端 perf lost samples/protocol errors/route drops 均为 0，双端 raw perf、reports、火焰图和 summary 位于 `/home/bubu/sdwan/target/v2-wan-local-wuwei-20260818-profile143-wire-accounting-wan/`；同 SHA 的 clean profile144 为 `2.261 Gbps`，双端 0 lost samples，证明 idle 修复未牺牲本机高速路径，产物位于 `/home/bubu/sdwan/target/v2-netns-20260818-profile144-idle-restart-clean/`。p2/p6 命名门槛继续独立保留。
- [x] 12 Mbit/s、双向 6 ms、8 流并发 ping 的 profile145–148 作为 falsifier 保留：profile145 在旧 send quantum 下 overlay `9.991 Mbps`、ping p95 `233 ms`；修正 bit/s 阈值和 live SMSS 后 profile146 为 `9.579 Mbps`、p95 `228 ms`，证明 send quantum 不是全部根因。随后发现 noq pacer 只有在 `bytes_to_send > quantum` 时才使用 BBR pacing rate，正常 MTU packet 实际退回 cwnd/RTT refill；改为控制器 rate 的真实 token bucket 后 profile147 的 QUIC RTT 从约 `90–220 ms` 降到约 `15 ms`，但 7 MiB 未分类 TUN backlog 把 ICMP 挡在 class-aware scheduler 之前，overlay/ping 反而为 `7.729 Mbps/2065 ms`。增加 hard-latency ingress lane 的 profile148 改善到 `8.089 Mbps/976 ms`，仍未达到验收门槛，因此 15.2 的 12 Mbit/s 项保持未完成，不能把局部改善写成通过。
- [x] BBR delivery-rate 生命周期又修正一层：每个 packet 的 `on_ack` 现在只累计 delivered/选择 newest packet，`on_end_acks` 生成完整 ACK-batch 的 interval/rate 后才执行一次 model/control update；首个 ACK 也计入 delivered，首个 sample 使用真实 `now-send_time` 而不是初始 RTT 占位值。定向测试闭合 `2,400 B / 11 ms` sample 并确认 max-bw 在 batch 完成后才更新；尚待隔离后的 netns/WAN profile 验证。
- [x] netns 与 subnet profile 已加入宿主管理面保护：全局单实例锁、默认 `99 Hz`、默认只使用进程实际允许 CPU 的后半区、`nice=10`、所有端点/iperf/perf/火焰图后处理共享 affinity，仅允许在本次 namespace veth 上安装 netem，并在启动/退出/HUP/TERM 前后比较 `200::/7`、`tun-ygg` link/address/qdisc，异常写入 diff 且退出码为 97。进一步定位出此前会直接冲击 SSH/Yggdrasil 的清理缺陷：端点 PID 为空时 `pgrep -P 0` 会选中宿主 PID 1/2，随后 privileged `kill -INT` 误向管理面发信号；现已删除 PID 0 fallback，任何 signal 前必须同时验证数值 PID、精确 perf argv 与精确 benchmark binary argv。两个脚本的 `PREFLIGHT_ONLY=1` 零流量门禁均已通过，创建/回收 namespace 后管理面快照 byte-identical、无残留 namespace/process；证据位于 `/home/bubu/sdwan/target/profile-v2-netns-pidguard-1787065912691440007/` 与 `/home/bubu/sdwan/target/profile-v2-subnet-netns-pidguard-1787065912960101772/`。负载测试仍须经过独立的低负载 canary，再逐级放量。
- [x] 最大合法 GSO Record 的接收预算现明确分离 IP payload 与有界 virtio metadata：旧预算只按 `65,536 B` payload ceiling 计账，合法 `65,535 B` GSO 加 metadata 会被误判为 reassembly 超限；现按 `max_train_size + max_cells_per_train × MAX_METADATA_BYTES` 进行 checked accounting，并有最大 GSO 回归测试。修复后 endpoint profile `target/v2-netns-20260818-profile146-v2only-metadata-budget-1787079456/` 达到 `3.186 Gbps`，双端 0 lost samples、0 ERROR/WARN。
- [x] subnet 满载断连已由任务级监督精确归因并根治：Bulk/Latency 各自未越过水位时，共享 8 MiB application budget 仍可能已满，旧 TX 会接纳同一批次第一个 PacketTrain、拒绝第二个并关闭整个 QUIC session。现增加 7 MiB shared application watermark，显式保留至少 1 MiB 以覆盖 `512 KiB + 1 GSO`、最强自动 `4+2` FEC 与 Cell envelope；单元测试锁定联合水位。精确产物 `sha256:e1c25d1dbae40ada6703b432c8c65c29096fbcde8640907af1f82cad0420d47f` 的 10 秒/4 流双栈 profile `target/p153-1787081643/` 完成 endpoint/subnet 正反向全矩阵：IPv4 endpoint `3.466/3.282 Gbps`、subnet `3.053/2.906 Gbps`，IPv6 endpoint `3.189/3.146 Gbps`、subnet `2.796/2.778 Gbps`；双端火焰图 0 lost samples、0 protocol error/route drop/ERROR/WARN，测试前后管理面快照 byte-identical。随后 binary `sha256:a30bde09cfaee929be520b71f104c61e667b811cc1f6b7056db48b506943cbe8` 又以 `target/p154-1787082213/` 完成相同双栈八向矩阵与双端火焰图，0 lost samples、0 ERROR/WARN。flat profile 当前首要用户态 self 成本为 `memmove 4.74%/6.00%` 与 AES-GCM open/seal，后续复制/AEAD 优化必须围绕这些已归因调用链，而不是再删状态机或盲换锁。
- [x] EndpointId 已完全退出 ClientHello 名称生成：vendored iroh 删除 `tls::name`、`.iroh.invalid` encoder 和两个客户端调用点，未显式选择 profile 的通用调用者也只使用 identity-neutral `media.example`；`scripts/check-v2-only.sh` 对此执行源码门禁。authority 选择的 `CoverConfig { profile_id, sni_pool }` 现进入签名 V2 invite，joiner 必须原样继承，签名正确但 generation/pool 非法的邀请也会拒绝；Docker 产品流用非默认 generation 17 和双域名池闭合 create→invite→join→daemon。mesh 任务监督新增 shutdown epoch，正常双端关闭不再把 control EOF 误报为运行时 WARN。最终精确产物 `ironetd sha256:6fdda2476b1b4811a306c3a8571356ef25076625fb0f05d0c88099f4d46b064c` 的双栈八向 profile 位于 `/home/bubu/sdwan/target/p156-clean-shutdown-1787084507/`：IPv4 endpoint `2.885/3.173 Gbps`、subnet `2.907/2.434 Gbps`，IPv6 endpoint `2.277/3.174 Gbps`、subnet `2.881/2.432 Gbps`；双端火焰图 0 lost samples、0 protocol error/route drop/ERROR/WARN，管理面快照 byte-identical。2 秒逐场景样本仅用于回归，不替代 30 秒实机性能结论。
- [x] 12 Mbit/s 根因实验已完成一轮收敛而未误报达标：当前稳定浅 admission 的 profile168 为 `7.911/10.108 Mbps = 78.27%`、ping p95 `42.7 ms`；关闭 TUN offload 的 profile171 提高到 `8.629/10.500 Mbps = 82.18%`，但 ping p95 恶化到 `170 ms`；PacketTrain 完成 affinity 的 profile172 只有 `80.47%`；自动 userspace GSO split 的 profile173 只有 `76.21%`。后两项实现均已回退。三轮发送端 CPU 仍仅约一成，且 Cell payload/wire 利用率接近 `97%`，证据把剩余根因收窄到 record-count TUN admission、内层 TCP 排队/重传及“已进入 QUIC 的 Bulk 无拥塞窗硬隔离”，而不是 memcpy、AEAD、更多 FEC 或 DRR 单次选择。下一步只接受 byte-bounded strict ingress/拥塞窗隔离并继续用双端火焰图证伪；15.2 的 `>=85%` 与延迟门槛保持未完成。
- [x] 12 Mbit/s 门槛现由端到端 byte-bound admission、严格 priority QUIC mailbox、可取消 Bulk quantum 抢占和 BBR 队列时延防护闭合：TUN 普通流量的 `512 KiB` permit 会跨 mesh dispatcher 一直持有到目标 adjacency scheduler 真正接纳，application watermark 自动夹到 live QUIC DATAGRAM buffer，Control/严格 Latency 可从被取消的 Bulk admission 前抢占；BBR 使用实测 min/SRTT，在半个 propagation RTT 的持续排队证据出现时自动从 Startup 进入 Drain或终止 ProbeBW Up，不增加用户参数，并累计 `controller_queue_delay_guard_transitions`。25% guard 的 profile183 证明延迟下降但容量仅 `81.12%`；自动放宽到半 RTT 后，同一 `12 Mbit/s` 双向、`6+6 ms`、8 流、50 ms ping 的 profile184/185/187 连续达到 `87.67%/88.81%/89.35%` 有效容量，双端 DWARF 火焰图均 0 lost samples。profile187 又为 underlay/overlay 同时采集并发 ping：underlay p95/p99 `186/187 ms`，overlay `46.4/118 ms`，overlay 增量为 `-139.6/-69.0 ms`，证明 V2 priority+pacing 没有把物理 FIFO bufferbloat冒充自身调度开销。FEC 关闭改为 fail-safe-off 后，profile188 的首个 10 秒 FEC wire 从 profile187 的 `559,710 B` 降到 `414,600 B`（`-25.9%`），整轮仍为 `8.819/10.326 Mbps = 85.41%`，overlay p95/p99 `45.8/101 ms`，相对匹配 underlay 为 `-142.2/-89.0 ms`。尝试把 controller transition 直接作为 tuner 单窗口拥塞输入的 profile189 为 `83.53%` 且未继续降低瞬时 FEC，已按火焰图/吞吐证据回退；保留只读 transition 遥测。最终已验证产物为 `/home/bubu/sdwan/target/v2-netns-20260820-profile188-immediate-fec-off/`（`ironetd sha256:780756db49e3782685b9bf492dbc0d1e13a7f68f14277e370b8d8a5d9eb9a53c`），matched-baseline 证据为 `/home/bubu/sdwan/target/v2-netns-20260820-profile187-matched-latency-baseline/`。
- [x] 8 流公平性已从 RSS bucket 假公平收敛到 flow 公平：profile191/192 将每个 TUN queue 固定为独立 admission/dispatcher 后虽把 ping p95 降到约 `24 ms`，但 TCP/UDP falsifier 均证明“每队列一份容量”会让碰撞到同一 RSS queue 的流量按碰撞数折半；profile195 的 8 条等负载 Bulk UDP 流 Jain 仅 `0.6254`。最终删除 per-RSS userspace service partition，在 class scheduler 前合并所有内核 TUN queue，并把小包持续流的自动 Bulk 晋升阈值降到适合 `12 Mbit/s / 8 flow` 的 `16 KiB + 128 KiB/s`，仍保留首包和 ICMP/TCP 控制语义的 Latency 保护。精确产物 `sha256:081d7727f9fd2889ce6de8502a17622e3c18d8c09f0f9036d32b57da5c1169ca` 的 profile198/199 均在 `12 Mbit/s`、双向 `6 ms`、8 条每流 `10 Mbit/s` 等负载 Bulk UDP 下通过：goodput 最大偏差分别 `8.47%/9.60%`，Jain `0.99782/0.99734`；profile199 同轮 TCP overlay/underlay 为 `9.036/10.297 Mbps = 87.75%`，overlay ping p95/p99 `39.8/86.7 ms`，双端 perf lost samples 为 0，测试结束后两端 `train_queue_bytes=0`、`latency_queue_bytes=0`。脚本现直接输出 delivered-rate 公平性和最终 class queue drain 断言；完整 JSON、日志、raw perf、reports 与双端火焰图位于 `/home/bubu/sdwan/target/v2-netns-20260820-profile198-bulk-fairness/` 与 `/home/bubu/sdwan/target/v2-netns-20260820-profile199-bulk-fairness-repeat/`。
- [x] 当前 V2-only 精确产物的双栈 endpoint/subnet 最终复测已闭合：`/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile203-final-repeat/` 使用同一 `sha256:081d7727f9fd2889ce6de8502a17622e3c18d8c09f0f9036d32b57da5c1169ca`、10 秒、8 流、499 Hz 双端 profile。IPv4 endpoint 正/反向为 `2.369/2.348 Gbps`，subnet 为 `2.185/2.130 Gbps`，比值 `92.22%/90.71%`；IPv6 endpoint 正/反向为 `2.327/2.173 Gbps`，subnet 为 `2.153/2.127 Gbps`，比值 `92.49%/97.90%`。双端 perf lost samples 均为 0，`a.svg`/`b.svg` 完整。profile200 的 IPv4 forward 单轮 `88.96%` 被 profile203 反证为采样波动；把 regular command mailbox 从 4 扩到 64 的 profile201/202 虽未破坏 subnet 吞吐，却使 8 流 Bulk 公平性恶化到最大偏差 `38.19%`、Jain `0.9077`，且发送端残留 `268,724 B` 队列，因此该实验已回退，保留深度 4 与全局 flow-aware DRR。subnet profile 现额外轮询正式 `status --output json`，把双端 class queue 归零作为停机和生成火焰图前的硬门禁，不再依赖 10 秒日志采样时机；同 SHA 的 `/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile204-queue-drain/` 已验证双端在首轮 100 ms 采样均归零、双端 perf lost samples 为 0 且火焰图完整。
- [x] trace/OAM source 关联进入正式 runtime 后用精确产物 `sha256:3f0ccaa7f739b3b3afef908e370c92c1226211fb095d0934fb69f95dce23714d` 完成热路径反证。`/home/bubu/sdwan/target/v2-netns-20260820-profile205-trace-hotpath/` 在 `12 Mbit/s`、双向 `6 ms`、30 秒 TCP + 20 秒 8-flow Bulk UDP 下仍为 `8.971/10.317 Mbps = 86.95%`，ping p95/p99 `27.6/86.3 ms`，公平性最大偏差 `9.90%`、Jain `0.99786`，双端队列归零且 perf lost samples 为 0；普通热路径只在已解析 UDP destination port 等于 trace port 时才进入 probe parser。`/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile206-trace-hotpath/` 的 IPv4 subnet/endpoint 正反向比值为 `95.86%/100.84%`，IPv6 为 `100.01%/91.51%`，双端队列归零、0 lost samples、双端火焰图完整。
- [x] 自动 LiveMedia 轮廓的阶跃负载暴露并修复了 BBR3 max-bw 窗口的周期语义错误：profile207 先以 `4 Mbit/s` UDP 激活 cover，再切到满载 TCP 后仅有 `1.450/10.484 Mbps = 13.83%`，因此作为 falsifier 保留。根因是 `ACKS_PROBE_STOPPING` 在整个 Down/Cruise 期间保持不变，使 `cycle_count` 每个 packet-timed RTT 都增长，名义“两轮 ProbeBW cycle”窗口实际在几十毫秒内过期。现在第一次非 app-limited stopping round 消费该 transition，下一轮 Refill/Up/Down 才重新武装，并有单测锁定一个 ProbeBW cycle 只增长一次 virtual time。精确产物 `sha256:38e067309eaf358a5215817aa770f252594a640424fb8dc42a2e95b508a81915` 的 `/home/bubu/sdwan/target/v2-netns-20260820-profile209-bbr-cycle/` 在相同 `12 Mbit/s`、双向 `6 ms`、30 秒 `4 Mbit/s` cover 激活、30 秒 8-TCP 满载和 20 秒 8-flow UDP 公平性下恢复到 `9.853/10.318 Mbps = 95.49%`；UDP 最大偏差 `6.21%`、Jain `0.99891`，active cover/Cell payload 为 `2.56%`，满载时自动降至 `0.010%`，主方向 full-payload Cell 占 `73.78%`，双端队列归零、perf lost samples 为 0 且火焰图完整。profile210 进一步尝试四槽 TUN slab ring，吞吐仍为 `94.86%`，但发送端 `memset` self 仅从 `21.46%` 变为 `20.55%`，没有达到可归因收益，已按 profile 结论回退，不以额外常驻 slab 换取无效复杂度。
- [x] 双栈 Gateway 的 reload/故障恢复矩阵已闭合。profile214 首先暴露 peer 做滚动 reload 时，本端会把对方主动关闭的旧 QUIC adjacency 当成 daemon 级致命错误；supervisor 现保持控制面存活并自动重建数据面 generation，恢复中的建连失败按固定 quiescence 窗口继续重试，不再把单节点升级扩散成 mesh-wide daemon exit。精确产物 `sha256:4c152af0635a232c9c969ca12fee62b2c16c16b0533035274ed902a033325ffd` 的 `/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile215-nat-reload-recovery/` 顺序 reload 两端后 IPv4/IPv6 forward ping 均通过，NAT/MASQUERADE/NAT66 owned chain 定义与 jump 各保持唯一，reload 前后规范化规则 diff 全为 0；IPv4/IPv6 subnet-to-endpoint 比值为 `93.40%/94.84%`，双端队列归零、perf lost samples 为 0 且火焰图完整。相同 SHA 的 `/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile216-route-reload-recovery/` 又以纯路由模式验证 reload 前后不存在 NAT owned chain，双栈正反向 ping/iperf 全部通过；IPv4 subnet 正/反向比值 `101.36%/94.54%`，IPv6 为 `92.10%/96.20%`，双端队列归零、0 lost samples、火焰图完整。规则比较只取 Ironet owned topology，排除 iptables-save 时间戳和实时包计数，避免用流量变化制造假失败。
- [x] V2 NAT 生命周期已从 QUIC/session generation 中解耦：daemon 存续期间使用 A/B owned chain 先完整构建新代、先挂 egress 再挂 ingress、最后回收旧代，peer loss 或数据面重建不再清空 conntrack 拓扑；只有 daemon 最终退出才全量清理。精确产物 `sha256:637d3b6248d04061ce4a2ce365777be95ee0c94cfe52d27721bfe4da6a6ace4a` 的 `/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile220-final-nat-reload/` 在顺序 reload 两端后保持双栈 ping、四组 owned-rule diff 为 0、MASQUERADE/NAT66 计数非零，IPv4/IPv6 subnet-to-endpoint 为 `105.05%/90.14%`，双端队列归零、0 lost samples 且双端火焰图完整；同一产物的 `/home/bubu/sdwan/target/v2-subnet-netns-20260820-profile221-final-routing-reload/` 验证纯路由 reload 全程无 owned NAT state，IPv4 正/反向为 `99.36%/95.31%`、IPv6 为 `102.28%/96.77%`。纯路由节点在系统未安装 iptables/ip6tables 时也不再把缺失的可选防火墙工具误判为数据面故障；正式 Docker SNI/EndpointId 正负向抓包矩阵复验通过。
- [x] LiveMedia QUIC 可见面现使用代码内唯一固定 transport 模板：QUIC v1、1200B initial/minimum MTU、8B source CID、20B 首次 destination CID、固定 stream/window/loss-detection/ACK policy、DATAGRAM capability、GSO 与 BBR3/PMTUD，网络自适应只改变 QUIC 内部 pacing/cwnd/PMTU 和 V2 admission，不改变握手 fingerprint。`tests/docker-v2-sni/lab.sh` 修复了“正常 pcap 为空但负向重试仍让聚合断言通过”的假阳性：现在等待 tcpdump ready、在 namespace 全接口抓取，并要求正常会话与错误 EndpointId 两份 pcap 各自存在 SNI=`cdn.live.example`、ALPN=`h3`、UDP Initial `>=1208B`、v1、20B DCID、8B SCID。精确产物 `sha256:eaaf4c2cd574138cf4eb127add6284336f8f07d3f6d443c862b575fba5e965cb` 的 `/home/bubu/sdwan/target/v2-netns-20260820-profile223-quic-template-30s/` 在 12 Mbit/s、12 ms、先 4 Mbit/s cover 后 30 秒 8-TCP 满载下达到 underlay 的 `97.00%`；active/saturated cover 为 `1.172%/0.0097%`，主方向 full-payload Cell `84.90%`，600 个并发 ping 零丢包、双端队列归零、perf lost samples 为 0 且火焰图完整。
- [x] TUN admission 现在只解析一次 IP/transport fixed header，并把 `PacketInfo` 与零复制 `Bytes`、byte-budget permit 作为同一 bounded descriptor 传到 route/class dispatcher；one-adjacency deferred queue 也保留 descriptor，不再释放 permit 后重新解析。稳定 Flow-hash 多 dispatcher 实验由精确产物 `sha256:5ecf3bbe0ab927241ab5c3c396d33b4a48e47e73801de68b44cd44fd617b1fc5` 的 `/home/bubu/sdwan/target/v2-netns-20260820-profile224-flow-shards-30s/` 证伪：8-flow Jain 从全局 admission 的近 1 降至 `0.79168`，因此实现已删除而非把吞吐换成 RSS bucket 假公平。最终精确产物 `sha256:a55b3004a3daa56c807e063bf55a3d0711be7ca5f2606a586070cb99a823ce6b` 的 `/home/bubu/sdwan/target/v2-netns-20260820-profile225-single-admission-parse-once-30s/` 在同一 12 Mbit/s、12 ms、cover→30 秒 8-TCP→8-flow UDP 矩阵达到 underlay 的 `96.32%`，公平性最大偏差 `3.13%`、Jain `0.999859`，600 个并发 ping 零丢包、双端队列归零、0 lost samples 且火焰图完整；同 SHA 的 `/home/bubu/sdwan/target/v2-netns-20260820-profile226-parse-once-unlimited/` 达到 `2.616 Gbit/s` overlay、8-flow Jain `0.9999994`、1000 个满载并发 ping 零丢包，主方向 full-payload Cell `96.42%`，双端队列归零、0 lost samples 且火焰图完整。实机访问复核 `/home/bubu/sdwan/target/v2-real-access-audit-20260820-parse-once/` 记录 p2/p6 运营商 IPv6 的 root/bubu 均仍在 SSH public-key admission 被拒绝；wuwei-ws 可达且到两者的路由均由 `enp5s0` 运营商 IPv6 默认网关承载、源地址为 `2408:8207:…`，未经过 `200::/7`，本轮未修改远端或注入负载。
- [x] profile226 的发送端 P-core/Atom `Mutex::lock_contended` self 为 `4.86%/3.57%`；临时按 connection lock purpose 做指数采样的精确产物 `sha256:41aedc858f11144c57d326255e4daf98a5ce5615b3ece2caa5a314904a04fce4` 在 `/home/bubu/sdwan/target/v2-netns-20260820-profile227-lock-purpose/` 证明，10 秒内仅仅读取 live QUIC DATAGRAM admission limit 就发生至少 `65,536` 次争用，远高于 `ReadDatagramBatch::poll` 的 `4,096` 次和 driver `poll` 的 `2,048` 次。临时诊断随后完整删除；noq `Shared` 现在用 `AtomicUsize` 发布已经由 protocol state 接受的当前 limit，set 路径仍在锁内执行原生收缩/唤醒语义，热路径读取只做 acquire load。最终精确产物 `sha256:d51b4ef937db37beded538490dc3b73ad15b718c30924084ba74a7dff70d1be1` 的 profile228/229 在与 profile226 相同的 20 秒、8-TCP、1000 ping、8-flow UDP 矩阵达到 `3.154/3.386 Gbit/s`，较 `2.616 Gbit/s` 紧邻基线提高 `20.56%/29.43%`；P-core mutex self 降至 `0.90%/1.27%`，Jain 为 `0.9999997/0.9999992`，满载 overlay ping p95 为 `1.49/1.55 ms`，双端队列归零、0 lost samples 且火焰图完整。相同 SHA 的 12 Mbit/s、12 ms、cover→30 秒 8-TCP profile230 仍为 underlay 的 `91.33%`、Jain `0.99818`、600 ping 零丢包并通过 `>=90%` 门槛；原始证据位于 `/home/bubu/sdwan/target/v2-netns-20260820-profile{228-atomic-datagram-limit,229-atomic-datagram-limit-repeat,230-atomic-limit-12mbit-30s}/`。
- [x] profile 脚本现在用 `perf record --sample-cpu` 保存每个样本的实际 CPU，并依据内核 `cpu_core/cpus`/`cpu_atom/cpus` 分别生成完整的 P-core/Atom `perf.script`、folded stack 和主火焰图，不再从会丢失 leaf/callchain 的 `perf report -g folded` 文本反推混合 PMU。精确旧产物 `sha256:d51b4ef937db37beded538490dc3b73ad15b718c30924084ba74a7dff70d1be1` 的 profile231 保留 30 秒双端分 PMU 原始栈；同 SHA 的 profile232 进一步用 Intel LBR 将 `memmove` 精确归因：QUIC DATAGRAM frame→packet 连续 encode 占总 cycles `1.586%`，V2 `CellV2::encode_into` 占 `0.818%`，其余主要来自 noq packet finish/RX handoff。基于调用图，TX 增加有界 `SegmentBufferPool`：本地无 FEC PacketTrain 的 `Vec<RecordSegment>` 在 Cell encode 后立即回收，下一 train 复用；builder 同时删除第二次 records collect、单 Record HashSet allocation，并按已知 Record 大小一次预留 Cell descriptor。最终精确产物 `sha256:5a41a6c5015d2b45303b1a69ffca84593d38cd1c4a9c2f14128c6dc898248e1d` 的 profile234 LBR 相对 profile232 将发送端 `__libc_malloc2` 从 `2.933%` 降到 `1.502%`（`-48.8%`）、`grow_amortized` 从 `2.576%` 降到 `0.630%`（`-75.5%`）、`RecordSegment` 路径从 `2.708%` 降到 `0.558%`（`-79.4%`）、整个 `build_packet_train` 从 `3.302%` 降到 `1.476%`（`-55.3%`）。紧邻相同 30 秒参数的 A/B profile237→238 为 `3.493→3.461 Gbit/s`（`-0.94%`，保守视为无吞吐增益），Jain 均近 1、1000 ping 零丢包、队列归零且双端 0 lost samples；独立 profile233/235 为 `3.393/3.503 Gbit/s`。同 SHA 的 12 Mbit/s、12 ms profile236 为 underlay 的 `90.85%`、600 ping 零丢包、Jain `1.0` 并继续通过门槛。完整原始数据和火焰图位于 `/home/bubu/sdwan/target/v2-netns-20260820-profile{231-sample-cpu-flames,232-lbr-callpaths,233-segment-pool,234-segment-pool-lbr,235-segment-pool-repeat,236-segment-pool-12mbit,237-segment-pool-ab-baseline,238-segment-pool-ab-new}/`。最新 `/home/bubu/sdwan/target/v2-real-access-audit-20260820-segment-pool/` 再次确认 p2/p6 的 root/bubu 均在运营商 IPv6 SSH public-key admission 被拒绝；wuwei-ws 可达，且到 p2/p6 都使用 `enp5s0`、源 `2408:8207:…` 的运营商 IPv6 默认路由而非 `200::/7`，本轮没有远端变更或负载测试。
- [x] 本地无 FEC、单 RecordSegment Cell 现以两段式 QUIC DATAGRAM 发送：scheduler 只编码并回收固定 Cell/segment/metadata prefix，Record data 保持原 TUN-owned `Bytes` view 直到 noq 最终构造连续 QUIC packet 并执行 AEAD；接收 wire、QUIC DATAGRAM frame、FEC parity、Repair cache 和 transit 可变 routing shim 均保持原连续语义，因此没有自研 UDP、没有协议兼容分支，也没有改变线上字节。精确新产物为 `ironetd sha256:478e130195fb493142d941454fe984d8bff86aaf02d25ca1647a76d5b523c491`。紧邻 30 秒 A/B profile239→240 为 `3.384516→3.384359 Gbit/s`（`-0.0047%`，吞吐等价），Jain `0.99999989→1.0`、1000 ping 零丢包、p95 `1.48→1.43 ms`、双端队列归零且 0 lost samples；matched 20 秒 LBR profile241→242 为 `3.493632→3.507600 Gbit/s`（`+0.40%`），发送端 flat `memmove` 从 `4.13%` 降至 `3.13%`（`-24.2%`）、旧 `CellV2::encode_into` self `0.87%` 在新报告中消失、`__libc_malloc2` 从 `0.44%` 降至 `0.34%`、`freeze_recycling` 从 `0.28%` 降至 `0.11%`。12 Mbit/s、双向 6 ms 的 profile243 达到 `10.395/10.951 Mbps = 94.92%`，4-flow Jain `1.0`、600 ping 零丢包、队列归零且双端 0 lost samples。完整 raw perf/report/folded/双端火焰图位于 `/home/bubu/sdwan/target/v2-netns-20260820-profile{239-scatter-ab-baseline,240-scatter-ab-new,241-scatter-lbr-baseline,242-scatter-lbr-new,243-scatter-12mbit}/`。最新 `/home/bubu/sdwan/target/v2-real-access-audit-20260820-scatter/` 仍记录 p2/p6 的 root/bubu 在运营商 IPv6 SSH public-key admission 被拒绝；wuwei-ws 到两者继续经 `enp5s0`、源 `2408:8207:…` 的运营商 IPv6 默认路由而非 `200::/7`，本轮未变更远端或注入负载。

## 1. 当前基线与需要消除的根因

- [x] 记录 p6 → p2 本机 overlay 基线：约 `829 Mbps`。
- [x] 记录 11.6.1.48 → p2 子网转发基线：约 `167–195 Mbps`。
- [x] 记录 MSS 1300 临时实验：约 `345 Mbps`，证明尾部分片是主要放大器。
- [x] 确认 1500B IP 包在当前有效 QUIC frame 下普遍产生两个 datagram。
- [x] 确认当前热路径主要成本包含 `calloc`、`memcpy`、ChaCha open/seal、virtio 和 IPI，而非单一状态机或互斥锁热点。
- [x] 确认当前 `/home/bubu/sdwan/src/runtime.rs` 会过早拆分 virtio GSO。
- [x] 确认当前 `/home/bubu/sdwan/src/wire.rs` 以单个 IP 包为边界独立分片。
- [ ] 将完整基线命令、原始 JSON、perf report 和火焰图位置写入 V2 性能记录。
- [ ] V2 开工前重新执行三轮 30 秒基线，排除机器和线路状态变化。

## 2. V2 不变量

- [x] IP Packet 只作为网络语义单位，不再作为调度、缓存和发送工作的唯一单位；正式运行时以 PacketTrain/Cell 为工作与 wire 单位。
- [x] V2 调度器以 Flow 作为分类和 Bulk 公平调度单位，并接入 immutable route snapshot 与 flow route lease。
- [x] Record 作为 PacketTrain 内的 IP 包描述单位。
- [x] Cell 作为接近 PMTU 的 wire DATAGRAM 载荷单位。
- [x] Stripe 作为有限大小的 FEC/Repair 单位。
- [x] Quantum 作为 V2 调度器最大不可抢占发送单位。
- [x] PacketTrain 作为聚合和内存所有权单位，不作为不可抢占发送单位。
- [x] Bulk、Latency、Control 数据禁止混入同一 PacketTrain，FEC Stripe 不跨 train、route、epoch 或 class。
- [x] V2 route-label 热路径不读取可变 Mesh/Presence 锁；只读取不可变 generation snapshot；完整多 peer Presence gossip、续租、撤销、topology 编译与 snapshot 发布均已接线。
- [x] 当前 V2 Session/Cell/Train/调度/reassembly/FEC/GSO 的长度、计数、偏移、分片数和内存均有硬上限。
- [x] QUIC 已提供的加密、重放防护、拥塞控制和 pacing 不在应用层重复实现；V2 应用层仅提供有界 admission、traffic-class scheduling、Cell/FEC/Repair 与 cover budget。

## 3. QUIC、SNI 与节点身份解耦

### 3.1 iroh TLS 接口

- [x] 维护 V2 使用的 iroh fork/patch，并固定到 crates.io `iroh 1.0.3` checksum。
- [x] 为连接选项增加 `visible_server_name = COVER_SNI`。
- [x] 为连接路径单独保留 `expected_endpoint_id = PEER_ID`（来自 `EndpointAddr`，不来自 SNI）。
- [x] 删除客户端连接路径对 `tls::name::encode(endpoint_id)` 的依赖；模块、调用点和 `.iroh.invalid` wire fallback 已物理删除并纳入 V2-only gate。
- [x] 将客户端证书验证器改为捕获 `expected_endpoint_id`。
- [x] 验证器直接比较服务端 raw public key 与预期 EndpointId。
- [x] `visible_server_name` 只进入 TLS ClientHello，不参与 EndpointId 推导。
- [x] 服务端继续从客户端 raw public key 验证和取得真实 EndpointId。
- [x] SNI 相同但 EndpointId 错误时，握手必须失败。
- [x] EndpointId 正确但 SNI 使用池中任一 `COVER_SNI` 时，握手必须成功。

### 3.2 ALPN 与加密首帧

- [x] 正式 V2 runtime 对外移除 `ironet/ip/1/...` ALPN；V1 daemon/runtime/wire 与协议选择器已从最终源码和产物删除。
- [x] V2 独立运行时不公开独立 probe ALPN。
- [x] V2 独立运行时对外只发送 `h3` ALPN。
- [x] 定义可靠控制流上的 `SessionHelloV2` 二进制格式和严格 limits 校验。
- [x] `SessionHelloV2` 至少包含 `network_id`、EndpointId、连接角色、Data/Probe 类型、wire limits、能力位和随机 challenge。
- [x] 将 `SessionHelloV2` 与 QUIC TLS exporter/当前连接绑定。
- [x] V2 TX/RX driver 只能由完成 network_id 校验后的 `NegotiatedSessionV2` 构造。
- [x] Probe 与 Data 使用相同握手可见面，仅在加密首帧中区分。

### 3.3 Session ticket 与 0-RTT

- [x] 不再依赖唯一 SNI 隔离 rustls session ticket。
- [x] 定义 `TicketKey = (expected_endpoint_id, network_id, cover_profile, quic_version)`。
- [x] 每个 peer/network 使用逻辑隔离的 resumption store。
- [x] 防止共享 `COVER_SNI` 的不同 peer 复用彼此 ticket。
- [x] V2 直接禁用 TLS 0-RTT（比“仅允许幂等请求”更严格）；vendored iroh 提供 endpoint 级开关，client 不发送 early data、server 将 `max_early_data_size` 设为 0，resumption ticket 仍可用于缩短完整握手。
- [x] Presence 更新、路由变更、NAT 状态和普通 IP 数据只在 1-RTT 完整握手及双向 SessionHello 完成后启用；`test_0rtt_disabled` 覆盖首次及恢复握手均不暴露 early-data connection。
- [ ] 测试 ticket 轮换、过期、错误 peer、错误 network 和路径迁移。

### 3.4 COVER_SNI 管理

- [x] 定义网络级 `COVER_SNI` 池，而非每个 peer 手工配置。
- [x] authority 通过签名 V2 invite 向新节点分发 cover profile generation 与完整 SNI pool；joiner 不使用本地默认值覆盖网络状态。
- [x] 连接建立时从当前池自动选择稳定值，连接存续期间不改变。
- [x] cover profile 变化只影响新连接，不中断现有 QUIC 会话。
- [x] 建连前并发、限时解析 SNI 池的 A/AAAA，优先选择与 peer direct IPv4/IPv6 locator 匹配的名称；DNS 超时或无匹配时回退到 network/peer/profile 稳定哈希，多个 peer 不会因串行 DNS 阻塞。
- [x] 状态输出显示 cover profile ID，不默认输出完整域名。
- [x] 配置和日志中禁止将 `COVER_SNI` 与 EndpointId 编码绑定。

## 4. QUIC Session 模型

- [x] 一个已认证 peer 对应一个逻辑 `PeerSessionV2`；direct、DERP 与路径迁移均留在同一端到端 QUIC connection 内。
- [x] 默认从一条 QUIC connection/lane 起步。
- [x] 删除应用层多 lane 自动扩展：单 peer 固定一条 QUIC connection，避免用并行 congestion controller 抢占链路；容量扩展交给同 connection 的 QUIC path migration/未来原生 multipath。
- [x] 因不存在并行 lane，无需应用层跨 connection 容量预算；所有 class 共享同一 QUIC congestion-control/pacing 状态与有界应用 admission。
- [x] 连接迁移不得替换逻辑 session、flow lease 或 delivery generation。
- [x] IPv4、运营商 IPv6、DERP 和已连接节点中转都实现统一 Path/Session 接口。
- [x] DERP 上继续承载端到端 QUIC；可靠底层路径自动关闭应用 FEC。
- [x] V2 driver 中可靠 Control Stream 与 DATAGRAM 使用独立队列，但共享同一 QUIC connection。
- [x] 未发送到 QUIC 的应用侧工作保持可抢占，只提交当前 bounded quantum。
- [x] 每条 connection 维护并输出 selected-path RTT/loss/cwnd/controller pacing/send quantum/PMTU、queue delay、migration epoch 和匿名 ticket partition 状态。

## 5. Wire V2：Record、Cell、PacketTrain

### 5.1 协议格式

- [x] 创建独立 `protocol/v2` 模块，不在 V1 编解码器中增加条件分支。
- [x] 固定整数端序、字段宽度、最大值和保留位规则。
- [x] 定义 `CellHeaderV2`：session epoch、route label、class、train ID、cell sequence、stripe ID、flags。
- [x] 定义 `RECORD_FULL`、`RECORD_START`、`RECORD_CONT`、`RECORD_END`。
- [x] `RECORD_START` 携带完整 IP 包长度和有界 metadata；delivery/GSO TLV 仍待定义。
- [x] Cell 允许紧接前一包尾部继续写入后续完整包，消除 IP 包边界尾部浪费。
- [x] 单个 IP 包允许跨多个 Cell。
- [x] 单个 Cell 允许承载多个完整或部分 IP Record。
- [x] Cell 自描述，丢失一个 Cell不阻塞其他已完整 Record 的交付。
- [x] 未知 flags、非法 segment 形状、超限长度和尾随字节必须快速拒绝。
- [x] V2 decoder 已有 nightly libFuzzer 入口；原始 wire 与有状态 FEC/Reassembly 序列均限制单输入、datagram 数量、单 datagram 大小、stripe/train 数和总内存，在任意输入下不得 panic、越界或无界分配。

### 5.2 PacketTrain

- [x] PacketTrain builder 通过单一 context 约束相同 route label、traffic class、session epoch 和 train ID。
- [x] 只聚合已经排队的数据；Latency 路径不固定等待凑批。
- [x] Bulk 初始 train 目标使用 32 KiB，运行时在 8–64 KiB 范围自动调整。
- [x] train builder 保存 `Bytes` segmented views，不拼接连续 train；Cell wire storage 由 `RecyclingBytePool` 循环复用，FEC wire storage 使用独立 pool，QUIC submit 只移动当前 bounded quantum。
- [x] 保存 Cell 发送 cursor，支持在任意 quantum 边界暂停和恢复。
- [x] PacketTrain 完成或超时后释放 V2 scheduler/reassembly 所有权；pooled permit 接线待完成。
- [x] 接收端按 Record 独立完成，禁止以 train 为单位阻塞已经完整的 Record。

### 5.3 GSO/GRO

- [x] V2 runtime 的 TUN RX 首先读取原始 virtio header，不调用会拆分 GSO 的 convenience API。
- [x] 校验 `gso_type`、`gso_size`、`hdr_len`、checksum start/offset 和总长度。
- [x] 支持 TCPv4、TCPv6、UDP L4 GSO 以及 checksum-only virtio metadata。
- [x] 有效 GSO super-packet 直接进入 PacketTrain，不先拆成 1500B 包。
- [x] 普通 1500B 包仍能自动连续打包进 Cell。
- [x] 接收端恢复原始 GSO/checksum metadata，并绕过会覆盖 virtio header 的二次 GRO。
- [x] 普通 Record 使用零复制 view/reassembly，并由 `send_multiple` 批量 GRO 写入 TUN。
- [x] GSO metadata 非法时只丢弃对应输入，不破坏 session。

## 6. 调度器与 Bulk 正式保证

### 6.1 流量分类

- [x] V2 分类器不存在 `packet.len() > 4 KiB => Bulk` 规则；包长只累计到持续流窗口。
- [x] V2 新 flow 默认进入 latency-protected 状态。
- [x] V2 根据持续时间、窗口字节数、发送速率、队列贡献和交互性自动晋升 Bulk。
- [x] V2 使用迟滞避免 flow 在 Latency/Bulk 之间频繁切换。
- [x] ICMP、TCP SYN/FIN/RST 与无 payload ACK 由解析语义强制保持 Latency；Control/OAM/握手走独立可靠 Control 层，不进入 IP Bulk 分类器。
- [x] 删除用户 DSCP/显式 class 依赖；默认完全由协议语义、持续时间、窗口字节、速率、队列贡献和交互性自动分类。
- [x] GSO super-packet 大小不直接决定 flow class；新流仍受 promotion age/window/rate 迟滞约束，并有 32 KiB TCP data 负向测试。

### 6.2 分层调度

- [x] 实现 `Control > Latency > Bulk > Probe/Padding` 的 V2 分层调度器。
- [x] Control 使用严格优先，并有独立内存上限；速率上限待自动调优接线。
- [x] Latency 根据 deadline/queue sojourn 调度。
- [x] Bulk 内部按 flow 使用 Deficit Round Robin。
- [x] Bulk 持续 backlog 时获得最低服务份额，不发生无限饥饿。
- [x] Priority 可以回收 Bulk elastic 内存，但不能吞掉 Bulk reserve。
- [x] Bulk 永远不能占用 Control/Latency reserve。
- [x] 新 Bulk flow 不能淘汰 Priority flow。
- [x] 当前 V2 peer/class 使用字节上限，Bulk flow 使用 DRR；per-flow admission 上限待接线。

### 6.3 抢占与发送 quantum

- [x] 单次 Bulk quantum 为 1–4 个 Cell。
- [x] 最大不可抢占提交量硬限制为 `<= 8 KiB`。
- [x] 每个 quantum 后必须重新检查 Control/Latency。
- [x] QUIC send queue 只提交当前 quantum，不提交整个 train。
- [x] FEC Stripe 允许在 Cell 边界暂停，不形成大型不可抢占区域。
- [x] V2 scheduler 记录 preemption、quantum bytes/count、最大 sojourn，并以固定 bucket 无锁直方图输出窗口化 p50/p95/p99。
- [x] 自动化测试断言大型 PacketTrain 在最多 4 Cell 后让新到达 Latency 获得调度。

## 7. 直播 QUIC Cover Profile

### 7.1 可见握手轮廓

- [x] profile 名称固定为 `LiveMedia`，不使用第三方产品名称。
- [x] 默认 UDP 端口支持 443，同时保留显式部署端口能力。
- [x] SNI 使用 `COVER_SNI`。
- [x] ALPN 只公开 `h3`。
- [x] 使用唯一代码内 QUIC v1/H3Media transport parameter 模板，不允许 peer 或用户逐连接改变握手 fingerprint。
- [x] Initial/minimum MTU、CID 长度、stream/window、loss-detection/ACK policy 和 DATAGRAM 能力在 profile 内固定；正常与错误身份两份独立 pcap 均逐项断言 wire shape。
- [x] 不再出现 `.iroh.invalid`、`ironet/ip/1` 或独立 probe ALPN。

### 7.2 1-RTT 包大小与节拍

- [x] Bulk 有数据时优先生成接近当前 PMTU 的完整 Cell。
- [x] 主方向持续高吞吐时使用 QUIC 原生 pacing，不额外 sleep。
- [x] 根据双向 EWMA 速率区分 LiveBroadcast、InteractiveVideo 和 GenericH3Bulk 轮廓。
- [x] 高度非对称大流使用“主方向数据 + QUIC 原生反向反馈”轮廓，不额外伪造反馈流。
- [x] 双向大流使用交互媒体轮廓，不强制伪造单向性。
- [x] packet-size bucket 只包含 128B small feedback、512B medium control、full PMTU 三类。
- [x] padding 只在一次真实发送后且 scheduler 已无 Control/Latency/Bulk 工作时产生。
- [x] 真实 Bulk 数据总是替换 padding。
- [x] 拥塞、丢包升高、CPU 饱和或队列增长时 padding budget 立即归零。
- [x] 低负载 cover overhead 上限为最近真实业务的 3%，并自动收缩。
- [x] 完全空闲时 padding 预算归零，只保留正常 QUIC keepalive。
- [x] cover shaper 位于 class scheduler 之后，padding 使用最低优先级 Probe class，不延迟 Control/Latency/Bulk。

### 7.3 Cover 可观测性

- [x] 结构化日志输出当前 cover profile、SNI pool generation、padding TX/RX bytes 和实际 cover overhead ratio，且不输出完整 SNI。
- [x] 分别统计真实数据、FEC、Repair、QUIC/Cell overhead 和 padding；profile137/138 已验证窗口分项闭合、Repair 双向字节严格对应且 accounting lag 为 0。
- [ ] cover profile 开启前后执行相同 iperf3/profile 对照。
- [ ] 满载吞吐差异目标 `<= 2%`。
- [ ] 满载 CPU 差异目标 `<= 3%`。

## 8. FEC 与 Repair V2

- [x] FEC 单位从完整 IP 包改为有限大小 Cell Stripe。
- [x] Stripe 使用 4–16 个 data Cell 范围，parity 数量按路径状态自动选择。
- [x] 不跨 traffic class、route label、path epoch 或 PacketTrain generation 建 Stripe。
- [x] parity 不要求原始 Cell padding 后再发送；只在 Reed-Solomon 编码输入中使用等长 symbol view。
- [x] 低丢包路径自动降到零冗余。
- [x] burst loss 增大时缩小 Stripe 并提高 parity。
- [x] 高 RTT 倾向预防性 FEC，低 RTT 保留轻量 Repair cache。
- [x] DERP/可靠 Stream path 自动关闭 FEC。
- [x] Repair request/response 使用可靠 Control Stream record。
- [x] 同一 Stripe 在一个接收生命周期内最多发出一次 Repair request，禁止在可靠响应到达前按固定 cooldown 重复放大控制流量。
- [x] Repair cache 以 Stripe/Train 为单位索引，不再为每个两片 1500B 包建独立生命周期。
- [x] FEC geometry 迟滞和 Repair cache 预算原子一致；降档不提前驱逐已承诺可修复的 Stripe，既有 Cell 保留到原 TTL。
- [x] 长期低丢包/无恢复收益时自动关闭 Repair cache。
- [x] FEC 输出并汇总 recovery latency、wasted parity、recovered cells、copy bytes、expired stripe、实际 parity wire bytes 和 Repair requested/received；对端累计反馈驱动本方向冗余调节。

## 9. 路由、Presence 与覆盖中转

- [x] 控制面将签名 Presence、前缀和双边健康路径编译成不可变 `DataplaneSnapshotV2`；多 peer gossip、ArcSwap publication、Kernel route reconcile 和定期续租已接线。静态前缀仍通过本地 Presence 输入同一编译器。
- [x] 单 peer与多 peer runtime 都在认证后交换严格有界的签名 Presence，编译双栈 node/prefix route generation，并原子 reconcile Kernel route；过期/撤销会同步删除 snapshot 与路由。
- [x] Flow 首包完成前缀查找，后续在 route epoch 内复用 `FlowRouteLeaseV2`；snapshot generation 变化时原子 refresh，并投递到 adjacency-bound TX worker。
- [x] topology compiler 在直接 owner link 健康且成本最优时编译单邻接快速路径，失效后切到健康 transit path。
- [x] 中转路径使用短 route label 和 O(1) FxHash label lookup。
- [x] transit fast path 不执行 Presence、前缀、Record、FEC 或 reassembly 扫描，只检查固定 Cell routing shim。
- [x] routed TX API 将一个单写者绑定到一个 adjacency，并要求每次 PacketTrain admission 的 `ResolvedRouteV2.adjacency` 匹配；整个 train 只携带同一 route label/epoch/hop-limit。
- [x] routed TX API 将 compiled route epoch 固定到整个 PacketTrain；snapshot 发布拒绝 epoch 复用并保留两个 retired generation，自然排空旧 Cell。
- [x] 定义外层 overlay hop count 与每个 Record 初始 TTL/Hop Limit 的关系：相同 TTL/Hop-Limit 的 Record 才进入同一 train，入口原值写入 Cell remaining-hop，traversed 初始为零。
- [x] 每个 transit hop 在固定头原位递减 remaining-hop、递增 traversed；共享 `Bytes` 才回退为一次 copy。
- [x] TTL/Hop Limit 在下一 transit hop 到零时生成带 snapshot/route/train/cell/reporter 的 OAM TTL-expired 控制记录。
- [x] `ironet trace` 已接入真实 route-label 跳点：每个探测使用内层 IP TTL/Hop-Limit 作为 Cell overlay budget，transit fast path 到期后沿可靠 OAM 反向返回 reporter；source runtime 以 `(route_epoch, route_label, train_id)` 将 OAM 严格关联到本次请求，再由 control stream 实时输出 `TraceHop`。transit 不解析 Record、不伪造源 IP/UDP 包，关联表上限 4,096、TTL 120 秒且首个完成事件清理同请求全部 train。错误 request/train、过期项、非 Presence reporter 和地址族不匹配均 fail closed；OAM→trace 与状态关联定向测试已通过。
- [x] label action 绑定 expected ingress/next adjacency，编译及运行时均阻止立即回送上一跳；更长环由 expected-ingress 约束和 overlay hop limit 双重封顶。

## 10. 子网 Gateway 与 NAT

- [x] blue-green runtime 已保留 advertised prefix、纯路由、IPv4 MASQUERADE 和 IPv6 NAT66 的实现语义；profile113–115 已完成双栈 namespace 纯路由/NAT66 验收，运营商实机门槛仍由 16.1 独立执行。
- [x] NAT 只标记 V2 TUN 入站连接，并只对目的属于本节点 advertised prefixes 的连接执行 MASQUERADE/NAT66。
- [x] Kernel Gateway 后端继续支持 nftables/conntrack/tcpdump 生态；profile215 通过 nft-compatible iptables-save 验证真实双栈 chain/jump/MASQUERADE 拓扑和非零计数。
- [x] Kernel Gateway 的本机纯路由路径使用多队列 TUN 并保留 GSO/GRO，已完成双栈 11.6.1.48 模拟拓扑与双栈 NAT 双端 profile；p2/p6 实机验收待完成。
- [ ] 设计可替换的 AF_XDP Gateway 后端作为 Linux 性能上限阶段。
- [ ] AF_XDP 后端提供等价的 NAT、策略、镜像、计数和抓包接口后才可成为默认。
- [x] NAT owned chain/conntrack 生命周期与 overlay route/session generation 解耦，只在配置语义变化时 reconcile、daemon 最终退出时清理。
- [x] reload 使用 A/B Gateway generation 无空窗替换，并由 profile220/221 断言 reload 前后 owned topology 唯一、规范化 diff 为 0、纯路由无残留。
- [x] `scripts/profile-v2-subnet-netns.sh` 将固定 `11.6.1.48 → p2-sim` 纯路由 reverse fast-path 与同方向 endpoint 基线放在同一精确产物、同一双端 profile 中；profile112 比值为 `101.5%`。真实 p2 三轮门槛仍由 16.1 独立验收。

## 11. 自动调优闭环

### 11.1 输入遥测

- [x] V2 tuner 输入 RTT、min RTT 和 queue delay；抖动字段接线待完成。
- [x] V2 tuner 输入 delivery rate；QUIC cwnd、bytes in flight 和 pacing rate 接线待完成。
- [x] V2 tuner 输入随机丢包、burst loss、重排和 path epoch。
- [x] V2 tuner 输入 PacketTrain/Latency queue bytes；Cell utilization 和 scheduler sojourn 汇总接线待完成。
- [x] V2 tuner 输入方向正确的 FEC recovery、wasted parity、matched Repair hit、完成请求数和 request→response latency；跨反馈周期分母按 matched requested Cells 对齐，路径变化后重新建立置信度。
- [x] V2 tuner 输入 CPU saturation；cycles/allocation/wakeup 指标接线待完成。
- [x] 每 adjacency 统计 TUN/Gateway ingress rate、累计 Record/字节、GSO ratio 和 average Record size，并由 profile83 验证发送/ACK 两个方向。

### 11.2 自动输出

- [x] 自动输出 PacketTrain 目标大小，并由 V2 TX 自动拆分 batch。
- [x] 自动输出最大聚合字节；Latency 固定无等待。
- [x] 自动输出 Bulk quantum 的 1–4 Cell 选择。
- [x] 自动输出 FEC data/parity 和 Stripe 大小。
- [x] 自动输出 Repair cache 启停与容量。
- [x] 删除未接入数据面的 QUIC lane 伪输出；每 adjacency 固定一条端到端 QUIC connection，由 connection manager 自动维护。
- [x] 自动输出 UDP/QUIC send/receive buffer 上下限；TX 已接入 noq-proto 原生 DATAGRAM accounting/event，RX Reassembly/FEC 总预算具备独立冷启动 floor、原位缩放和非致命 pressure shedding，并由 profile74/75 验证决策值与实际值一致。
- [x] QUIC RX 使用无等待、按协商硬上限的 buffered burst drain；已删除会增加锁读却不减少等待的动态 wakeup 伪输出，TUN 写入按已完成 Record 批量合并。
- [x] 自动输出 cover padding budget，并已接入 LiveMedia token-bucket shaper。
- [x] TUN admission batch 按实际 ingress rate、平均 Record 大小和 GSO ratio 自动输出 1–64 条目标；GSO 元数据保持端到端，不以用户配置拆分或重写 GRO geometry。

### 11.3 控制稳定性

- [x] FEC 使用快速保护环，batch/buffer 使用渐进性能环；cover 使用双向速率 EWMA，拥塞/CPU/backlog/loss 安全门即时归零。
- [x] FEC 输出带最小样本、迟滞和冷却时间，batch/buffer 每次只移动一个 step。
- [x] path epoch 改变时清理旧估计并保守冷启动。
- [x] `AutoTuneBoundsV2` 只表达策略上限，不直接指定瞬时最优值。
- [x] 每 10 个样本的结构化状态日志显示当前值、选择原因、样本年龄和策略上下限。
- [x] 路径遥测暂时不可用时立即清空学习状态并回到固定有界保守 profile；保留逻辑 path epoch、等待遥测恢复，不退出 tuner 或无限扩大 buffer/batch/FEC。

## 12. 并发、内存与复制

- [ ] 多队列 TUN/RX 队列与 CPU worker 稳定绑定。
- [ ] Flow 在 route epoch 内固定归属一个单写者 worker。
- [ ] worker 独占本 shard 的 flow、builder、FEC 和 pacing staging 状态。
- [ ] shard 间只传递批次描述符或共享只读 snapshot。
- [x] V2 runtime 的 TUN、Control 和 Repair 路径只使用有界 MPSC/watch channel。
- [x] 大型 TUN receive batch 先进入有界 staging，再逐 Record admission，保留至少 1 MiB scheduler headroom，满载不因批次越界关闭 session。
- [ ] buffer budget 按 Control、Latency、Bulk、Reassembly 和 FEC 分账。
- [ ] pooled buffer 在 TX build、QUIC submit、RX decode 和 TUN write 之间复用。
- [x] 接收端优先使用 slice/view，不复制完整 Record。
- [x] TUN ingress 在 reader 只解析一次 IP/transport header；下游 route/classifier 复用随 bounded descriptor 携带的 `PacketInfo`，不再对同一 Record 重复 header walk。
- [x] live QUIC DATAGRAM admission limit 使用 connection-local 原子 snapshot；只在自动调优真正改变 limit 时进入 QUIC state lock，PacketTrain admission 高频读取不再争用 driver mutex。
- [x] 本地无 FEC PacketTrain 的 RecordSegment descriptor Vec 使用有界跨 train 回收池；Cell wire encode 完成立即归还，FEC/Repair/转发仍保留完整连续 wire ownership，不改变协议或恢复语义。
- [x] 本地无 FEC、单 segment Cell 使用 `DatagramPayload { head, tail }` 保留 Record data view；只在 QUIC packet builder/AEAD 边界执行必需的最终连续 copy，多 segment、FEC、Repair 与 transit 自动回退连续 Cell ownership。
- [x] 对跨 Cell Record 提供专用两片 descriptor inline fast path；第三片起自动 spill 到堆，兼顾普通 MTU 与大 GSO Record。
- [ ] 批量更新计数器、队列深度和 TUN wakeup；QUIC DATAGRAM 已按 1–4 Cell bounded quantum 合并一次状态锁。
- [x] profile 中持续跟踪 `calloc`、`memset`、`memcpy`、ChaCha/AES open/seal 和 futex；profile34–79 的双端 report/火焰图均保留这些符号与前后对照。

## 13. 安全与协议健壮性

- [x] EndpointId raw public key 仍是 peer 身份根。
- [x] SessionHello 绑定 network_id、peer IDs、TLS exporter、随机 challenge 和能力摘要。
- [x] session、route label、train 和 Stripe 均绑定 epoch。
- [x] 旧 epoch Cell 不进入新 generation 重组状态。
- [x] 限制同时活跃 train/reassembly/Stripe 数量和总字节。
- [x] 对重复 Cell、乱序、超时、恶意长度和稀疏 sequence 建立明确处理规则。
- [x] V2 RX 只能在 SessionHello 完成后构造，且超限 DATAGRAM 在大内存分配前拒绝。
- [x] FEC decode 前验证 symbol 数量、原始长度表、parity index 和 symbol 长度上限。
- [x] fuzz wire decoder、SessionHello、route/OAM、GSO metadata、Presence、Repair、feedback、cover 和有状态 FEC/Reassembly decoder。
- [x] QUIC DATAGRAM 协议解析错误与 route-label gate drop 使用独立累计指标；首个及后续 2 的幂次事件才写日志，持续畸形流量为 O(log n) 日志量且不会因单个坏 DATAGRAM 终止已认证 session。

## 14. 可观测性

- [x] 新增 flow class service bytes、PacketTrain/Record/Cell build、FEC Stripe/protected/parity/tail/copy、Latency/Bulk quantum 和自动 cover profile 维度；区分 build、实际 wire send 与对端 receive 三层计数。
- [x] 输出实际 wire `cell_payload_utilization`，并单独输出 PacketTrain build payload utilization，避免把 FEC/头部混入分母。
- [x] 输出 `cells_per_megabyte` 和 `records_per_train`。
- [x] 输出 `gso_input_bytes`、`gso_preserved_bytes` 和 `gso_fallback_splits`；仅真正的 TCP/UDP GSO super-packet 计入，checksum-only metadata 不误计，当前原生 metadata 路径保持零 userspace fallback split。
- [x] 输出真实已提交 wire bytes 的 `bulk_service_share_ppm`、无锁 32-bucket flow sketch 的 Jain `bulk_fairness_ppm`，以及 Bulk 被 Latency 抢占的次数、平均/最大排队延迟；发送热路径只做原子加法，不引入 flow-map 锁。
- [x] 使用固定 13-bucket 无锁直方图输出窗口化 `latency_queue_sojourn` p50/p95/p99，不保存每包样本或做热路径排序。
- [x] 输出实际 FEC wire overhead、对端 recovered/wasted parity 和双向 Repair requested/received，避免用名义 geometry 代替真实发送成本。
- [x] 输出 cover padding TX/RX bytes/ratio，但不把完整 `COVER_SNI` 作为高基数字段。
- [x] 输出匿名 network digest + cover generation + QUIC version 的 session ticket partition、明确的 0-RTT disabled/accepted/rejected 计数和 connection-local migration epoch，不暴露原始 network name 或 SNI。
- [x] peer、QUIC path、签名 Presence、路由、Gateway/NAT、队列、丢弃和错误均进入实时 status/peers、CLI/TUI 与 V2 原生 Prometheus 输出；不再读取旧 metrics 文件。
- [x] 当前本机双端 profile 采集未报告 lost samples；p2/p6/wuwei-ws 每轮仍须检查并记录。

## 15. 测试矩阵

### 15.1 单元、属性和 fuzz

- [x] Cell/Record 编解码 round-trip。
- [x] 单 Cell 多 Record、单 Record 多 Cell，以及跨 Cell Record 的两片 inline descriptor fast path。
- [x] Cell 重复、乱序、独立 Record 完成、超时回收、pressure shedding 和最多三个并存 route epoch。
- [x] PacketTrain pause/resume 和 class 隔离。
- [x] GSO metadata 合法/非法边界及 checksum-only metadata。
- [x] FEC 0 parity、单 Cell 丢失、乱序和 parity 数量范围内 burst loss。
- [x] Bulk quantum 抢占、DRR 公平首轮和最低服务份额。
- [x] ticket cache 不跨 peer/network/profile/version。
- [x] COVER_SNI 相同但 EndpointId 错误时握手失败。
- [x] 真实 loopback QUIC 上完成 `h3` + cover SNI + SessionHelloV2 + Control Stream + PacketTrain DATAGRAM + RX 重组端到端测试。
- [x] 真实双 namespace QUIC 上完成 bounded batch、scheduler admission headroom、双端 task-clock report/火焰图验收，持续满载无控制连接异常关闭。
- [x] route label 错误入口/立即反射、TTL/Hop Limit 和 OAM expiry。
- [x] 所有 V2 协议 decoder 均进入 `v2_wire_decoders` 或 `v2_stateful_receive` fuzz target，并带有效 Session/Cell/OAM/Route/Repair/cover/feedback 种子 corpus。

### 15.2 namespace/Docker 集成

- [x] 本机 namespace IPv4 direct underlay 与 IPv4 overlay/subnet；运营商实机路径待完成。
- [ ] 运营商 IPv6 direct，并排除 `200::/7` overlay 地址。
- [ ] NAT 映射变化与 QUIC connection migration。
- [x] DERP-only 路径。
- [x] 已连接 peer overlay transit；三 namespace A→B→C ping 0% loss，4 流 15 秒 release profile 为 `1.862 Gbps`。
- [x] 双栈 advertised subnet 的 NAT 与纯路由 namespace 模式均已通过：profile113 验收双向纯路由，profile114/115 在 LAN 无回程路由时以非零规则计数验收 MASQUERADE/NAT66。
- [x] PMTU 变化与 1500B 包边界：已完成端到端 path-min 编译、live shrink 可靠 OAM 反馈和不关闭 session 的负载回归；ICMP 黑洞/恢复矩阵仍待实机验收。
- [x] 12 Mbit/s、12 ms、8 条 TCP Bulk 流与并发 ping；profile209 达到有效容量 `95.49%`，并保留 matched ping、8-flow UDP fairness 与双端火焰图。
- [x] FEC 自动 tune 的随机丢包和 burst loss 场景；profile58/59/66/138 已覆盖随机丢包收敛、低冗余和 Repair 命中，burst-loss decoder/调优边界由单测覆盖。
- [x] 抓取 QUIC Initial，确认只出现 `COVER_SNI` 和 `h3`。
- [ ] cover profile 满载开关对照。

### 15.3 p2/p6/wuwei-ws 实机验收

- [ ] 每个里程碑在 p2/p6 部署同一精确 commit 和构建产物 SHA。
- [ ] 每次测试记录 underlay 地址、overlay 地址、路由、MTU、CPU governor 和进程版本。
- [ ] p2 ↔ p6 underlay 三轮 30 秒 iperf3。
- [ ] p2 ↔ p6 overlay endpoint 三轮 30 秒 iperf3。
- [ ] p2 → 11.6.1.48 和 11.6.1.48 → p2 各三轮 30 秒 iperf3。
- [ ] p2 ↔ wuwei-ws 使用公网运营商 IPv6，不经过 `200::/7`。
- [ ] p2 → wuwei-ws 执行 FEC off/on/auto 三组丢包链路验收。
- [ ] 每轮同步采集 p2 与对端 task-clock profile 和火焰图。
- [ ] 每轮记录吞吐、重传、RTT、loss、Cell/MB、utilization、FEC、padding、CPU 和 context switch。
- [ ] 测试后恢复生产二进制、路由、防火墙和临时 sysctl，并校验 SHA。

## 16. 性能与功能验收门槛

### 16.1 数据面

- [x] codec 自动测试中 `44 × 1500B` Cell 平均载荷利用率 `>95%`；实机遥测待接线。
- [ ] 子网 reverse 的 datagram/MB 相比 V1 下降 `>= 40%`。
- [ ] p2 → p6 overlay 达到同方向 underlay 的 `>= 90%`。
- [ ] p2 → 11.6.1.48 与 p2 → p6 overlay 差距 `<= 10%`。
- [ ] 11.6.1.48 → p2 达到 p6 → p2 endpoint 的 `>= 90%`，按当前基线目标约 `>= 750 Mbps`。
- [ ] cover profile 开启前后满载吞吐差距 `<= 2%`。
- [ ] profile 中 allocation/memcpy/wakeup 每 MB 相比 V1 显著下降并记录百分比。
- [ ] 连续三轮结果稳定，无控制连接异常关闭和无效样本混入结论。

### 16.2 Bulk 与延迟隔离

- [x] 最大不可抢占提交量 `<= 8 KiB`。
- [x] Priority 到达后最迟在下一个 quantum 获得调度。
- [x] Bulk backlog 持续存在时不发生无限饥饿。
- [x] 8 条 Bulk flow 的 goodput 偏差 `<= 10%`。
- [x] 12 Mbit/s 限速场景 Bulk goodput `>= 85%` 有效容量。
- [x] ping p95/p99 增量符合一个 quantum 加调度周期开销的预算；matched underlay 基线下增量实际为负。
- [x] 测试结束后所有 class 队列归零。

### 16.3 SNI/直播轮廓

- [x] QUIC Initial 中不存在 `.iroh.invalid`。
- [x] QUIC Initial 中不存在 `ironet/ip/1` 或 probe ALPN。
- [x] SNI 为当前 `COVER_SNI`，ALPN 仅为 `h3`。
- [x] SNI 相同的错误 peer 无法通过 EndpointId raw public key 验证。
- [x] session ticket 不跨 peer/network/profile/version 混用，并以独立 resumption cache 测试覆盖相同 SNI 的错误 peer/network。
- [x] Bulk 时 full-PMTU packet 占主要比例；profile209 主方向 full-payload Cell 为 `73.78%`。
- [x] padding 在拥塞和真实 backlog 下自动归零；profile209 满载窗口仅剩边界异步账本内 `0.010%`，最终 decision budget 为 0。
- [x] cover overhead 保持在自动预算内；profile209 低负载实测 `2.56%`，低于自动 `3%` 上限。

### 16.4 功能等价

- [x] 静态 peer、动态签名 Presence、周期续租、过期/成员撤销和 Kernel 前缀删除。
- [x] IPv4/IPv6 direct、DERP、overlay transit；双栈 namespace、公开多 DERP current-source 测试和三节点 real-QUIC transit 均已通过。
- [ ] NAT traversal、地址变化和连接恢复。
- [x] advertised subnet NAT 与纯路由的本机双栈功能/reload/故障恢复矩阵；profile215/216 顺序 reload 后规则唯一、双向连通且数据面自动恢复，p2/p6 性能门槛仍由 16.1 独立验收。
- [x] 默认路由隔离和 underlay 防递归；默认 `/0` 拒绝、opt-in 与 underlay exclusion 已由编译器/runtime 测试覆盖。
- [x] 自动 FEC、Repair、容量估计和路径选择。
- [x] ping、trace、status、TUI、Prometheus 和热重载。
- [ ] 服务启停、升级、回滚和故障后状态清理。

## 17. 实现阶段

### Phase 0：冻结规范和基线

- [x] 固化本文件中的 wire limits、Bulk 不变量和验收门槛。
- [x] 旧实现的实机基线仅作为本文件中的历史对照冻结；新版不部署、不构建也不携带 V1 binary/runtime 以补采样本。
- [x] 建立不进入发布包的 `ironet_v2_lab` example、独立 TUN/端口和测试命令入口。
- [ ] 为每个 Phase 建立可重复 benchmark 脚本。

### Phase 1：QUIC 身份/SNI 重构

- [x] 完成 iroh patch/fork。
- [x] 解耦 visible SNI 与 expected EndpointId。
- [x] V2 独立运行时 ALPN 切换为 `h3`。
- [x] 实现 SessionHelloV2 和 ticket partition。
- [x] 完成握手、0-RTT、迁移和抓包测试；0-RTT 双端禁用、direct/relay migration、NAT rebinding 和 QUIC Initial SNI/ALPN 均有 current-source 自动测试。

### Phase 2：Wire V2 Codec

- [x] 完成 Cell/Record/Train/Stripe 格式。
- [x] 完成 decoder hard limits、属性测试和 fuzz；`nix develop .#fuzz --command scripts/fuzz-v2.sh` 提供固定 nightly/cargo-fuzz 环境，首轮双 target 各 1,000 次 ASan/libFuzzer smoke 均通过，wire/stateful 覆盖分别从空 corpus 的 203/143 提升到有效种子下的 821/580 edges。
- [x] 完成 route label、overlay TTL/Hop-Limit 和 TTL-expired OAM metadata；trace CLI 展示待 Phase 6。

### Phase 3：TX PacketTrain 与 Bulk Scheduler

- [x] 实现 flow 分类、PacketTrain builder、有界 burst 聚合和 Cell/FEC wire recycling pool。
- [x] 实现分层调度、DRR、reserve 和 `<=8 KiB` quantum。
- [x] 正式 runtime 的 V2 driver 已接入 QUIC DATAGRAM 且禁止整 train 预灌。
- [x] 完成本机双 namespace V2 TX profile；p2/p6 实机 profile 待 Phase 9 验收。

### Phase 4：RX、GSO/GRO 与零复制

- [x] 实现 Cell decode、独立 Record 完成和两片 descriptor 内联 fast path；超过两片自动 spill 并保留 gather-write。
- [x] 保留并恢复原始 TUN GSO/checksum metadata。
- [x] 实现 per-epoch reassembly descriptor storage 复用、QUIC DATAGRAM batch wakeup、普通包 TUN batch write 与 GSO fragmented gather-write；payload 生命周期直接复用 QUIC-owned `Bytes`，不另建全局 pool。
- [x] 完成本机双 namespace V2 RX profile并确认接收端 futex/reassembly 仍是后续热点；p2/p6 实机 profile 待 Phase 9 验收。

### Phase 5：FEC/Repair V2

- [x] 实现 Cell Stripe FEC。
- [x] 实现可靠 Stream Repair codec、请求生成和 Stripe cache。
- [x] 正式 runtime 已接入方向正确的 V2 自动 tune、真实 QUIC/队列/CPU 遥测与 Repair cache 自适应。
- [ ] 使用 p2 → wuwei-ws 丢包链路验收。

### Phase 6：路由与中转 V2

- [x] 实现 snapshot、route label 和 transit fast path；多 peer connection manager、Presence gossip/compiler、adjacency writer、FEC-safe 原位转发和 real-QUIC transit 已接线并 profile。
- [x] 实现 TTL/OAM/trace：TTL/OAM wire、disposition、可靠逐跳反向中继、source train/request 关联和 `ironet trace` 流式 `TraceHop` 展示均已完成；实机 transit 输出并入 Phase 9 命名节点矩阵复核。
- [x] 验证直连、中转、DERP 和路径迁移。

### Phase 7：Subnet Gateway V2

- [x] V2 runtime 已完成多队列 TUN 原始 GSO endpoint 与 IPv4 纯路由 subnet 路径；NAT、完整 advertised-prefix 控制面和 transit 已接入，命名实机门槛待完成。
- [x] 完成 NAT/纯路由功能等价；profile215/216 已覆盖 IPv4/IPv6、MASQUERADE/NAT66、纯路由、Presence/route generation reload 和 peer 故障恢复。
- [ ] 完成 p2 ↔ 11.6.1.48 双向性能门槛。
- [ ] 根据 profile 决定 AF_XDP backend 的进入条件。

### Phase 8：LiveMedia Cover

- [x] 完成 cover profile、DNS 感知 SNI pool、固定 H3Media transport 模板和自动 padding budget。
- [x] 完成方向性轮廓识别与自动 padding shaper。
- [x] 完成正常/错误身份双 pcap 可见握手断言与 packet-size/Cell-utilization/cover-overhead 直方图验收。
- [ ] 验证 cover 开销不破坏 Bulk 和 p2/p6 性能。

### Phase 9：全量回归与切换

- [ ] 通过全部单元、fuzz、Docker/netns 和实机矩阵。
- [ ] 在所有节点部署同一 V2 commit。
- [x] 正式 `ironetd` 入口直接切换到 V2 generation，不保留双协议选择器。
- [x] 生产 `ironet-v2` binary 已删除；两个 profile 驱动均通过正式 `ironet` 生成/密封 V2 产品配置，并只启动正式 `ironetd`。
- [x] 删除 V1 runtime、wire、配置项、Docker 场景和死代码；回滚只允许回滚整个软件版本，不在新版中携带 V1。

## 18. Kill List

- [x] 删除 Wire V1 编解码和版本 fallback。
- [x] 删除一包一个 `TransmissionJob` 的 Bulk 默认路径。
- [x] 删除一包一个普通 repair cache 生命周期。
- [x] 删除以 IP 包边界为必须边界的 wire fragmentation。
- [x] 删除 `>4 KiB => Bulk`。
- [x] 删除公开 `network_id`/probe 自定义 ALPN。
- [x] 删除 EndpointId 编码 SNI。
- [x] 删除以 ServerName 作为唯一 ticket 隔离键的实现。
- [x] 删除用户直接控制 batch、buffer、lane、FEC、MSS 和 wakeup 的常规配置。
- [x] 删除热路径 Mesh/Presence 锁和重复 route lookup。
- [x] 删除先拆 GSO 再尝试聚合的路径。
- [x] 删除整 PacketTrain 一次性提交 QUIC 的实现可能性。

## 19. Definition of Done

- [ ] 所有 Phase checklist 完成，未完成项有明确删除决定而非静默跳过。
- [ ] 功能等价矩阵全部通过。
- [ ] p2/p6、p2/11.6.1.48、p2/wuwei-ws 实机门槛全部通过。
- [ ] 每个关键性能提交都有双端火焰图和可重复原始数据。
- [x] SNI/ALPN 可见面和 EndpointId 身份隔离均通过抓包与负向测试。
- [ ] Bulk 抢占、最低服务份额、公平性和满载利用率都有自动化断言。
- [ ] 自动调优在低损、高损、高 RTT、带宽变化和路径迁移场景稳定收敛。
- [ ] 生产节点升级、切换、整体回滚和状态清理流程演练完成。
- [x] V1 wire、V1 配置兼容层和相关死代码从最终产物移除。
